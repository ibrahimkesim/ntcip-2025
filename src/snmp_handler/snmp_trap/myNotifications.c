/*
 * Note: this file originally auto-generated by mib2c
 * using mib2c.notify.conf
 */

#include "log.h"
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>
#include "myNotifications.h"

extern const oid snmptrap_oid[];
extern const size_t snmptrap_oid_len;

int send_trafficLightStatus_trap(int idx, int chnNo, int greenStatus, int yellowStatus, int redStatus)
{
    netsnmp_variable_list  *var_list = NULL;
    const oid myROIntHit_oid[] = { 1,3,6,1,4,1,59873,4,2,1,11,1 };
    const oid trafficLightFaultIndex_oid[] = { 1,3,6,1,4,1,59873,4,2,1,2,2,1,1, /* insert index here */ };
    const oid trafficLightFaultChannelNumber_oid[] = { 1,3,6,1,4,1,59873,4,2,1,2,2,1,2, /* insert index here */ };
    const oid trafficLightFaultGreenStatus_oid[] = { 1,3,6,1,4,1,59873,4,2,1,2,2,1,3, /* insert index here */ };
    const oid trafficLightFaultYellowStatus_oid[] = { 1,3,6,1,4,1,59873,4,2,1,2,2,1,4, /* insert index here */ };
    const oid trafficLightFaultRedStatus_oid[] = { 1,3,6,1,4,1,59873,4,2,1,2,2,1,5, /* insert index here */ };

    /*
     * Set the snmpTrapOid.0 value
     */
    snmp_varlist_add_variable(&var_list,
        snmptrap_oid, snmptrap_oid_len,
        ASN_OBJECT_ID,
        myROIntHit_oid, sizeof(myROIntHit_oid));
    
    /*
     * Add any objects from the trap definition
     */
    snmp_varlist_add_variable(&var_list,
        trafficLightFaultIndex_oid, OID_LENGTH(trafficLightFaultIndex_oid),
        ASN_INTEGER,
        /* Set an appropriate value for trafficLightFaultIndex */
        &idx, sizeof(int));
    snmp_varlist_add_variable(&var_list,
        trafficLightFaultChannelNumber_oid, OID_LENGTH(trafficLightFaultChannelNumber_oid),
        ASN_INTEGER,
        /* Set an appropriate value for trafficLightFaultChannelNumber */
        &chnNo, sizeof(int));
    snmp_varlist_add_variable(&var_list,
        trafficLightFaultGreenStatus_oid, OID_LENGTH(trafficLightFaultGreenStatus_oid),
        ASN_INTEGER,
        /* Set an appropriate value for trafficLightFaultGreenStatus */
        &greenStatus, sizeof(int));
    snmp_varlist_add_variable(&var_list,
        trafficLightFaultYellowStatus_oid, OID_LENGTH(trafficLightFaultYellowStatus_oid),
        ASN_INTEGER,
        /* Set an appropriate value for trafficLightFaultYellowStatus */
        &yellowStatus, sizeof(int));
    snmp_varlist_add_variable(&var_list,
        trafficLightFaultRedStatus_oid, OID_LENGTH(trafficLightFaultRedStatus_oid),
        ASN_INTEGER,
        /* Set an appropriate value for trafficLightFaultRedStatus */
        &redStatus, sizeof(int));

    /*
     * Add any extra (optional) objects here
     */
    log_trace("TRAP! Traffic light status: %d %d %d %d %d" , idx, chnNo, greenStatus, yellowStatus, redStatus);

    /*
     * Send the trap to the list of configured destinations
     *  and clean up
     */
    send_v2trap( var_list );
    snmp_free_varbind( var_list );
    usleep(20*1000);

    return SNMP_ERR_NOERROR;
}

int send_cabinetDevTrap_trap(int  devType, int devStatus)
{
    netsnmp_variable_list  *var_list = NULL;
    const oid myROIntHit2_oid[] = { 1,3,6,1,4,1,59873,4,2,1,11,2 };
    const oid cabinetEnvironDeviceType_oid[] = { 1,3,6,1,4,1,1206,4,2,1,12,2,1,2, /* insert index here */ };
    const oid cabinetEnvironDeviceOnStatus_oid[] = { 1,3,6,1,4,1,1206,4,2,1,12,2,1,5, /* insert index here */ };

    /*
     * Set the snmpTrapOid.0 value
     */
    snmp_varlist_add_variable(&var_list,
        snmptrap_oid, snmptrap_oid_len,
        ASN_OBJECT_ID,
        myROIntHit2_oid, sizeof(myROIntHit2_oid));
    
    /*
     * Add any objects from the trap definition
     */
    snmp_varlist_add_variable(&var_list,
        cabinetEnvironDeviceType_oid, OID_LENGTH(cabinetEnvironDeviceType_oid),
        ASN_INTEGER,
        /* Set an appropriate value for cabinetEnvironDeviceType */
        &devType, sizeof(int));
    snmp_varlist_add_variable(&var_list,
        cabinetEnvironDeviceOnStatus_oid, OID_LENGTH(cabinetEnvironDeviceOnStatus_oid),
        ASN_INTEGER,
        /* Set an appropriate value for cabinetEnvironDeviceOnStatus */
        &devStatus, sizeof(int));

    /*
     * Add any extra (optional) objects here
     */
    log_trace("TRAP! Cabinet Device: %d %d" , devType, devStatus);

    /*
     * Send the trap to the list of configured destinations
     *  and clean up
     */
    send_v2trap( var_list );
    snmp_free_varbind( var_list );
    usleep(10*1000);

    return SNMP_ERR_NOERROR;
}

int send_ascLineVoltsTrap_trap(int volt)
{
    netsnmp_variable_list  *var_list = NULL;
    const oid ascLineVoltsTrap_oid[] = { 1,3,6,1,4,1,59873,4,2,1,11,3 };
    const oid ascLineVolts_oid[] = { 1,3,6,1,4,1,1206,4,2,1,12,8, 0 };

    /*
     * Set the snmpTrapOid.0 value
     */
    snmp_varlist_add_variable(&var_list,
        snmptrap_oid, snmptrap_oid_len,
        ASN_OBJECT_ID,
        ascLineVoltsTrap_oid, sizeof(ascLineVoltsTrap_oid));
    
    /*
     * Add any objects from the trap definition
     */
    snmp_varlist_add_variable(&var_list,
        ascLineVolts_oid, OID_LENGTH(ascLineVolts_oid),
        ASN_INTEGER,
        /* Set an appropriate value for ascLineVolts */
        &volt, sizeof(volt));

    /*
     * Add any extra (optional) objects here
     */
    log_trace("TRAP! Line Volts: %d" , volt);

    /*
     * Send the trap to the list of configured destinations
     *  and clean up
     */
    send_v2trap( var_list );
    snmp_free_varbind( var_list );
    usleep(10*1000);

    return SNMP_ERR_NOERROR;
}

int send_detectorVehicleTrap_trap( int grpNo, int grpAlarm )
{
    netsnmp_variable_list  *var_list = NULL;
    const oid detectorVehicleTrap_oid[] = { 1,3,6,1,4,1,59873,4,2,1,11,4 };
    const oid vehicleDetectorStatusGroupNumber_oid[] = { 1,3,6,1,4,1,1206,4,2,1,2,4,1,1, /* insert index here */ };
    const oid vehicleDetectorStatusGroupAlarms_oid[] = { 1,3,6,1,4,1,1206,4,2,1,2,4,1,3, /* insert index here */ };

    /*
     * Set the snmpTrapOid.0 value
     */
    snmp_varlist_add_variable(&var_list,
        snmptrap_oid, snmptrap_oid_len,
        ASN_OBJECT_ID,
        detectorVehicleTrap_oid, sizeof(detectorVehicleTrap_oid));
    
    /*
     * Add any objects from the trap definition
     */
    snmp_varlist_add_variable(&var_list,
        vehicleDetectorStatusGroupNumber_oid, OID_LENGTH(vehicleDetectorStatusGroupNumber_oid),
        ASN_INTEGER,
        /* Set an appropriate value for vehicleDetectorStatusGroupNumber */
        &grpNo, sizeof(int));
    snmp_varlist_add_variable(&var_list,
        vehicleDetectorStatusGroupAlarms_oid, OID_LENGTH(vehicleDetectorStatusGroupAlarms_oid),
        ASN_INTEGER,
        /* Set an appropriate value for vehicleDetectorStatusGroupAlarms */
        &grpAlarm, sizeof(int));

    /*
     * Add any extra (optional) objects here
     */
    log_trace("TRAP! Detector Vehicle: %d %d" , grpNo, grpAlarm);

    /*
     * Send the trap to the list of configured destinations
     *  and clean up
     */
    send_v2trap( var_list );
    snmp_free_varbind( var_list );
    usleep(10*1000);

    return SNMP_ERR_NOERROR;
}

int send_detectorPedestrianTrap_trap( int grpNo, int grpAlarm )
{
    netsnmp_variable_list  *var_list = NULL;
    const oid detectorPedestrianTrap_oid[] = { 1,3,6,1,4,1,59873,4,2,1,11,5 };
    const oid pedestrianDetectorStatusGroupNumber_oid[] = { 1,3,6,1,4,1,1206,4,2,1,2,9,1,1, /* insert index here */ };
    const oid pedestrianDetectorStatusGroupAlarms_oid[] = { 1,3,6,1,4,1,1206,4,2,1,2,9,1,3, /* insert index here */ };

    /*
     * Set the snmpTrapOid.0 value
     */
    snmp_varlist_add_variable(&var_list,
        snmptrap_oid, snmptrap_oid_len,
        ASN_OBJECT_ID,
        detectorPedestrianTrap_oid, sizeof(detectorPedestrianTrap_oid));
    
    /*
     * Add any objects from the trap definition
     */
    snmp_varlist_add_variable(&var_list,
        pedestrianDetectorStatusGroupNumber_oid, OID_LENGTH(pedestrianDetectorStatusGroupNumber_oid),
        ASN_INTEGER,
        /* Set an appropriate value for pedestrianDetectorStatusGroupNumber */
        &grpNo, sizeof(int));
    snmp_varlist_add_variable(&var_list,
        pedestrianDetectorStatusGroupAlarms_oid, OID_LENGTH(pedestrianDetectorStatusGroupAlarms_oid),
        ASN_INTEGER,
        /* Set an appropriate value for pedestrianDetectorStatusGroupAlarms */
        &grpAlarm, sizeof(int));

    /*
     * Add any extra (optional) objects here
     */
    log_trace("TRAP! Detector Pedestrian: %d %d" , grpNo, grpAlarm);

    /*
     * Send the trap to the list of configured destinations
     *  and clean up
     */
    send_v2trap( var_list );
    snmp_free_varbind( var_list );
    usleep(10*1000);

    return SNMP_ERR_NOERROR;
}

int send_driverModuleTrap_trap( int moduleStatus )
{
    netsnmp_variable_list  *var_list = NULL;
    const oid driverModuleTrap_oid[] = { 1,3,6,1,4,1,59873,4,2,1,11,6 };
    const oid driverModuleStatus_oid[] = { 1,3,6,1,4,1,59873,4,2,1,1,1, 0 };

    /*
     * Set the snmpTrapOid.0 value
     */
    snmp_varlist_add_variable(&var_list,
        snmptrap_oid, snmptrap_oid_len,
        ASN_OBJECT_ID,
        driverModuleTrap_oid, sizeof(driverModuleTrap_oid));
    
    /*
     * Add any objects from the trap definition
     */
    snmp_varlist_add_variable(&var_list,
        driverModuleStatus_oid, OID_LENGTH(driverModuleStatus_oid),
        ASN_INTEGER,
        /* Set an appropriate value for driverModuleStatus */
        &moduleStatus, sizeof(int));

    /*
     * Add any extra (optional) objects here
     */
    log_trace("TRAP! Driver Module: %d" , moduleStatus);

    /*
     * Send the trap to the list of configured destinations
     *  and clean up
     */
    send_v2trap( var_list );
    snmp_free_varbind( var_list );
    usleep(10*1000);

    return SNMP_ERR_NOERROR;
}


int send_signalStateExecModeChange_trap(int modeVal)
{
    netsnmp_variable_list  *var_list = NULL;
    const oid signalStateExecModeTrap_oid[] = { 1,3,6,1,4,1,59873,4,2,1,11,7};
    const oid signalStateExecMode_oid[] = { 1,3,6,1,4,1,59873,4,2,1,4,2,1,3, /* insert index here */ };

    /*
     * Set the snmpTrapOid.0 value
     */
    snmp_varlist_add_variable(&var_list,
        snmptrap_oid, snmptrap_oid_len,
        ASN_OBJECT_ID,
        signalStateExecModeTrap_oid, sizeof(signalStateExecModeTrap_oid));
    
    /*
     * Add any objects from the trap definition
     */
    snmp_varlist_add_variable(&var_list,
        signalStateExecMode_oid, OID_LENGTH(signalStateExecMode_oid),
        ASN_INTEGER,
        /* Set an appropriate value for ascLineVolts */
        &modeVal, sizeof(modeVal));

    /*
     * Add any extra (optional) objects here
     */
    log_trace("TRAP! Exec Mode Changed: %d" , modeVal);

    /*
     * Send the trap to the list of configured destinations
     *  and clean up
     */
    send_v2trap( var_list );
    snmp_free_varbind( var_list );
    usleep(10*1000);

    return SNMP_ERR_NOERROR;
}


int send_dedectorModuleTrap_trap( int moduleStatus )
{
    netsnmp_variable_list  *var_list = NULL;
    const oid dedectorModuleTrap_oid[] = { 1,3,6,1,4,1,59873,4,2,1,11,8 };
    const oid kgmdedectorModuleStatus_oid[] = { 1,3,6,1,4,1,59873,4,2,1,6,1, 0 };
    /*
     * Set the snmpTrapOid.0 value
     */
    snmp_varlist_add_variable(&var_list,
        snmptrap_oid, snmptrap_oid_len,
        ASN_OBJECT_ID,
        dedectorModuleTrap_oid, sizeof(dedectorModuleTrap_oid));
    
    /*
     * Add any objects from the trap definition
     */
    snmp_varlist_add_variable(&var_list,
        kgmdedectorModuleStatus_oid, OID_LENGTH(kgmdedectorModuleStatus_oid),
        ASN_INTEGER,
        /* Set an appropriate value for driverModuleStatus */
        &moduleStatus, sizeof(int));

    /*
     * Add any extra (optional) objects here
     */
    log_trace("TRAP! Dedector Module: %d" , moduleStatus);

    /*
     * Send the trap to the list of configured destinations
     *  and clean up
     */
    send_v2trap( var_list );
    snmp_free_varbind( var_list );
    usleep(10*1000);
    return SNMP_ERR_NOERROR;
}


int send_otherModuleTrap_trap( int moduleStatus )
{
    netsnmp_variable_list  *var_list = NULL;
    const oid otherModuleTrap_oid[] = { 1,3,6,1,4,1,59873,4,2,1,11,9 };
    const oid otherModuleStatus_oid[] = { 1,3,6,1,4,1,59873,4,2,1,6,2, 0 };

    /*
     * Set the snmpTrapOid.0 value
     */
    snmp_varlist_add_variable(&var_list,
        snmptrap_oid, snmptrap_oid_len,
        ASN_OBJECT_ID,
        otherModuleTrap_oid, sizeof(otherModuleTrap_oid));
    
    /*
     * Add any objects from the trap definition
     */
    snmp_varlist_add_variable(&var_list,
        otherModuleStatus_oid, OID_LENGTH(otherModuleStatus_oid),
        ASN_INTEGER,
        /* Set an appropriate value for driverModuleStatus */
        &moduleStatus, sizeof(int));

    /*
     * Add any extra (optional) objects here
     */
    log_trace("TRAP! Other Module: %d" , moduleStatus);

    /*
     * Send the trap to the list of configured destinations
     *  and clean up
     */
    send_v2trap( var_list );
    snmp_free_varbind( var_list );
    usleep(10*1000);

    return SNMP_ERR_NOERROR;
}