/*
 * Note: this file originally auto-generated by mib2c
 * using mfd-data-get.m2c
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "globalModuleTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement globalModuleTable get routines.
 * TODO:240:M: Implement globalModuleTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table globalModuleTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * NTCIP1201-v03::globalModuleTable is subid 3 of globalConfiguration.
 * Its status is Mandatory.
 * OID: .1.3.6.1.4.1.1206.4.2.6.1.3, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement globalModuleTable data context functions.
 */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 * @param moduleNumber_val
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
globalModuleTable_indexes_set_tbl_idx(globalModuleTable_mib_index *tbl_idx, long moduleNumber_val)
{
    DEBUGMSGTL(("verbose:globalModuleTable:globalModuleTable_indexes_set_tbl_idx","called\n"));

    /* moduleNumber(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/R/d/h */
    tbl_idx->moduleNumber = moduleNumber_val;
    

    return MFD_SUCCESS;
} /* globalModuleTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
globalModuleTable_indexes_set(globalModuleTable_rowreq_ctx *rowreq_ctx, long moduleNumber_val)
{
    DEBUGMSGTL(("verbose:globalModuleTable:globalModuleTable_indexes_set","called\n"));

    if(MFD_SUCCESS != globalModuleTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , moduleNumber_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != globalModuleTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* globalModuleTable_indexes_set */


/*---------------------------------------------------------------------
 * NTCIP1201-v03::moduleTableEntry.moduleDeviceNode
 * moduleDeviceNode is subid 2 of moduleTableEntry.
 * Its status is Mandatory, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.1206.4.2.6.1.3.1.2
 * Description:
<Definition>This object contains the device node number of the
device-type, e.g., an ASC signal controller would have an OID of
1.3.6.1.4.1.1206.4.2.1.
<Object Identifier> 1.3.6.1.4.1.1206.4.2.6.1.3.1.2
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OBJECTID (based on perltype OBJECTID)
 * The net-snmp type is ASN_OBJECT_ID. The C type decl is oid (oid)
 * This data type requires a length.
 */
/**
 * Extract the current value of the moduleDeviceNode data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param moduleDeviceNode_val_ptr_ptr
 *        Pointer to storage for a oid variable
 * @param moduleDeviceNode_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by moduleDeviceNode.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*moduleDeviceNode_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update moduleDeviceNode_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
moduleDeviceNode_get( globalModuleTable_rowreq_ctx *rowreq_ctx, oid **moduleDeviceNode_val_ptr_ptr, size_t *moduleDeviceNode_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != moduleDeviceNode_val_ptr_ptr) && (NULL != *moduleDeviceNode_val_ptr_ptr));
   netsnmp_assert( NULL != moduleDeviceNode_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:globalModuleTable:moduleDeviceNode_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the moduleDeviceNode data.
 * copy (* moduleDeviceNode_val_ptr_ptr ) data and (* moduleDeviceNode_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for moduleDeviceNode data
     */
    if ((NULL == (* moduleDeviceNode_val_ptr_ptr )) ||
        ((* moduleDeviceNode_val_ptr_len_ptr ) <
         (rowreq_ctx->data.moduleDeviceNode_len* sizeof(rowreq_ctx->data.moduleDeviceNode[0])))) {
        /*
         * allocate space for moduleDeviceNode data
         */
        (* moduleDeviceNode_val_ptr_ptr ) = malloc(rowreq_ctx->data.moduleDeviceNode_len* sizeof(rowreq_ctx->data.moduleDeviceNode[0]));
        if(NULL == (* moduleDeviceNode_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.moduleDeviceNode)\n");
            return MFD_ERROR;
        }
    }
    (* moduleDeviceNode_val_ptr_len_ptr ) = rowreq_ctx->data.moduleDeviceNode_len* sizeof(rowreq_ctx->data.moduleDeviceNode[0]);
    memcpy( (* moduleDeviceNode_val_ptr_ptr ), rowreq_ctx->data.moduleDeviceNode, rowreq_ctx->data.moduleDeviceNode_len* sizeof(rowreq_ctx->data.moduleDeviceNode[0]) );

    return MFD_SUCCESS;
} /* moduleDeviceNode_get */

/*---------------------------------------------------------------------
 * NTCIP1201-v03::moduleTableEntry.moduleMake
 * moduleMake is subid 3 of moduleTableEntry.
 * Its status is Mandatory, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.1206.4.2.6.1.3.1.3
 * Description:
<Definition>This object specifies the manufacturer of the
associated module. A null-string shall be transmitted if this
object has no entry.
<Object Identifier> 1.3.6.1.4.1.1206.4.2.6.1.3.1.3
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the moduleMake data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param moduleMake_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param moduleMake_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by moduleMake.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*moduleMake_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update moduleMake_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
moduleMake_get( globalModuleTable_rowreq_ctx *rowreq_ctx, char **moduleMake_val_ptr_ptr, size_t *moduleMake_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != moduleMake_val_ptr_ptr) && (NULL != *moduleMake_val_ptr_ptr));
   netsnmp_assert( NULL != moduleMake_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:globalModuleTable:moduleMake_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the moduleMake data.
 * copy (* moduleMake_val_ptr_ptr ) data and (* moduleMake_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for moduleMake data
     */
    if ((NULL == (* moduleMake_val_ptr_ptr )) ||
        ((* moduleMake_val_ptr_len_ptr ) <
         (rowreq_ctx->data.moduleMake_len* sizeof(rowreq_ctx->data.moduleMake[0])))) {
        /*
         * allocate space for moduleMake data
         */
        (* moduleMake_val_ptr_ptr ) = malloc(rowreq_ctx->data.moduleMake_len* sizeof(rowreq_ctx->data.moduleMake[0]));
        if(NULL == (* moduleMake_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.moduleMake)\n");
            return MFD_ERROR;
        }
    }
    (* moduleMake_val_ptr_len_ptr ) = rowreq_ctx->data.moduleMake_len* sizeof(rowreq_ctx->data.moduleMake[0]);
    memcpy( (* moduleMake_val_ptr_ptr ), rowreq_ctx->data.moduleMake, rowreq_ctx->data.moduleMake_len* sizeof(rowreq_ctx->data.moduleMake[0]) );

    return MFD_SUCCESS;
} /* moduleMake_get */

/*---------------------------------------------------------------------
 * NTCIP1201-v03::moduleTableEntry.moduleModel
 * moduleModel is subid 4 of moduleTableEntry.
 * Its status is Mandatory, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.1206.4.2.6.1.3.1.4
 * Description:
<Definition>This object specifies the model number (hardware) or
firmware reference (software) of the associated module. A null-
string shall be transmitted if this object has no entry.
<Object Identifier> 1.3.6.1.4.1.1206.4.2.6.1.3.1.4
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the moduleModel data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param moduleModel_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param moduleModel_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by moduleModel.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*moduleModel_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update moduleModel_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
moduleModel_get( globalModuleTable_rowreq_ctx *rowreq_ctx, char **moduleModel_val_ptr_ptr, size_t *moduleModel_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != moduleModel_val_ptr_ptr) && (NULL != *moduleModel_val_ptr_ptr));
   netsnmp_assert( NULL != moduleModel_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:globalModuleTable:moduleModel_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the moduleModel data.
 * copy (* moduleModel_val_ptr_ptr ) data and (* moduleModel_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for moduleModel data
     */
    if ((NULL == (* moduleModel_val_ptr_ptr )) ||
        ((* moduleModel_val_ptr_len_ptr ) <
         (rowreq_ctx->data.moduleModel_len* sizeof(rowreq_ctx->data.moduleModel[0])))) {
        /*
         * allocate space for moduleModel data
         */
        (* moduleModel_val_ptr_ptr ) = malloc(rowreq_ctx->data.moduleModel_len* sizeof(rowreq_ctx->data.moduleModel[0]));
        if(NULL == (* moduleModel_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.moduleModel)\n");
            return MFD_ERROR;
        }
    }
    (* moduleModel_val_ptr_len_ptr ) = rowreq_ctx->data.moduleModel_len* sizeof(rowreq_ctx->data.moduleModel[0]);
    memcpy( (* moduleModel_val_ptr_ptr ), rowreq_ctx->data.moduleModel, rowreq_ctx->data.moduleModel_len* sizeof(rowreq_ctx->data.moduleModel[0]) );

    return MFD_SUCCESS;
} /* moduleModel_get */

/*---------------------------------------------------------------------
 * NTCIP1201-v03::moduleTableEntry.moduleVersion
 * moduleVersion is subid 5 of moduleTableEntry.
 * Its status is Mandatory, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.1206.4.2.6.1.3.1.5
 * Description:
<Definition>This object specifies the version of the associated
module. If the moduleType has a value of software, the value of
this object shall include the date on which the software was
released as a string in the form of YYYYMMDD, it shall be followed
by a space, a hyphen, another space, the lower-case letter ‘v’,
followed by a version or configuration number. Preceding zeros
shall be required for the date. For example, version 7.03.02 of
the software released on July 5, 2002 would be presented as
20020705 – v7.03.02
A null-string shall be transmitted if this object has no entry.
<Object Identifier> 1.3.6.1.4.1.1206.4.2.6.1.3.1.5
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the moduleVersion data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param moduleVersion_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param moduleVersion_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by moduleVersion.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*moduleVersion_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update moduleVersion_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
moduleVersion_get( globalModuleTable_rowreq_ctx *rowreq_ctx, char **moduleVersion_val_ptr_ptr, size_t *moduleVersion_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != moduleVersion_val_ptr_ptr) && (NULL != *moduleVersion_val_ptr_ptr));
   netsnmp_assert( NULL != moduleVersion_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:globalModuleTable:moduleVersion_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the moduleVersion data.
 * copy (* moduleVersion_val_ptr_ptr ) data and (* moduleVersion_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for moduleVersion data
     */
    if ((NULL == (* moduleVersion_val_ptr_ptr )) ||
        ((* moduleVersion_val_ptr_len_ptr ) <
         (rowreq_ctx->data.moduleVersion_len* sizeof(rowreq_ctx->data.moduleVersion[0])))) {
        /*
         * allocate space for moduleVersion data
         */
        (* moduleVersion_val_ptr_ptr ) = malloc(rowreq_ctx->data.moduleVersion_len* sizeof(rowreq_ctx->data.moduleVersion[0]));
        if(NULL == (* moduleVersion_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.moduleVersion)\n");
            return MFD_ERROR;
        }
    }
    (* moduleVersion_val_ptr_len_ptr ) = rowreq_ctx->data.moduleVersion_len* sizeof(rowreq_ctx->data.moduleVersion[0]);
    memcpy( (* moduleVersion_val_ptr_ptr ), rowreq_ctx->data.moduleVersion, rowreq_ctx->data.moduleVersion_len* sizeof(rowreq_ctx->data.moduleVersion[0]) );

    return MFD_SUCCESS;
} /* moduleVersion_get */

/*---------------------------------------------------------------------
 * NTCIP1201-v03::moduleTableEntry.moduleType
 * moduleType is subid 6 of moduleTableEntry.
 * Its status is Mandatory, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.1206.4.2.6.1.3.1.6
 * Description:
<Definition>This object specifies whether the associated module
is a hardware or software module.
<Object Identifier> 1.3.6.1.4.1.1206.4.2.6.1.3.1.6
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 * Enum range: 3. Values:  other(1), hardware(2), software(3)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the moduleType data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param moduleType_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
moduleType_get( globalModuleTable_rowreq_ctx *rowreq_ctx, u_long * moduleType_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != moduleType_val_ptr );


    DEBUGMSGTL(("verbose:globalModuleTable:moduleType_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the moduleType data.
 * copy (* moduleType_val_ptr ) from rowreq_ctx->data
 */
    (* moduleType_val_ptr ) = rowreq_ctx->data.moduleType;

    return MFD_SUCCESS;
} /* moduleType_get */



/** @} */
