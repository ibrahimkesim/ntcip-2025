/*
 * Note: this file originally auto-generated by mib2c
 * using mfd-data-get.m2c
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "channelStatusGroupTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement channelStatusGroupTable get routines.
 * TODO:240:M: Implement channelStatusGroupTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table channelStatusGroupTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * NTCIP1202-v03::channelStatusGroupTable is subid 4 of channel.
 * Its status is Mandatory.
 * OID: .1.3.6.1.4.1.1206.4.2.1.8.4, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement channelStatusGroupTable data context functions.
 */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 * @param channelStatusGroupNumber_val
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
channelStatusGroupTable_indexes_set_tbl_idx(channelStatusGroupTable_mib_index *tbl_idx, long channelStatusGroupNumber_val)
{
    DEBUGMSGTL(("verbose:channelStatusGroupTable:channelStatusGroupTable_indexes_set_tbl_idx","called\n"));

    /* channelStatusGroupNumber(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/R/d/h */
    tbl_idx->channelStatusGroupNumber = channelStatusGroupNumber_val;
    

    return MFD_SUCCESS;
} /* channelStatusGroupTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
channelStatusGroupTable_indexes_set(channelStatusGroupTable_rowreq_ctx *rowreq_ctx, long channelStatusGroupNumber_val)
{
    DEBUGMSGTL(("verbose:channelStatusGroupTable:channelStatusGroupTable_indexes_set","called\n"));

    if(MFD_SUCCESS != channelStatusGroupTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , channelStatusGroupNumber_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != channelStatusGroupTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* channelStatusGroupTable_indexes_set */


/*---------------------------------------------------------------------
 * NTCIP1202-v03::channelStatusGroupEntry.channelStatusGroupReds
 * channelStatusGroupReds is subid 2 of channelStatusGroupEntry.
 * Its status is Mandatory, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.1206.4.2.1.8.4.1.2
 * Description:
<Definition> Channel Red Output Status Mask, when a bit=1,
the Channel Red is currently active. When a bit=0, the Channel
Red is NOT currently active.
Bit 7: Channel # = (channelStatusGroupNumber * 8)
Bit 6: Channel # = (channelStatusGroupNumber * 8) - 1
Bit 5: Channel # = (channelStatusGroupNumber * 8) - 2
Bit 4: Channel # = (channelStatusGroupNumber * 8) - 3
Bit 3: Channel # = (channelStatusGroupNumber * 8) - 4
Bit 2: Channel # = (channelStatusGroupNumber * 8) - 5
Bit 1: Channel # = (channelStatusGroupNumber * 8) - 6
Bit 0: Channel # = (channelStatusGroupNumber * 8) - 7
<Object Identifier> 1.3.6.1.4.1.1206.4.2.1.8.4.1.2
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   0
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the channelStatusGroupReds data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param channelStatusGroupReds_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
channelStatusGroupReds_get( channelStatusGroupTable_rowreq_ctx *rowreq_ctx, long * channelStatusGroupReds_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != channelStatusGroupReds_val_ptr );


    DEBUGMSGTL(("verbose:channelStatusGroupTable:channelStatusGroupReds_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the channelStatusGroupReds data.
 * copy (* channelStatusGroupReds_val_ptr ) from rowreq_ctx->data
 */
    (* channelStatusGroupReds_val_ptr ) = rowreq_ctx->data.channelStatusGroupReds;

    return MFD_SUCCESS;
} /* channelStatusGroupReds_get */

/*---------------------------------------------------------------------
 * NTCIP1202-v03::channelStatusGroupEntry.channelStatusGroupYellows
 * channelStatusGroupYellows is subid 3 of channelStatusGroupEntry.
 * Its status is Mandatory, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.1206.4.2.1.8.4.1.3
 * Description:
<Definition> Channel Yellow Output Status Mask, when a
bit=1, the Channel Yellow is currently active. When a bit=0, the
Channel Yellow is NOT currently active.
Bit 7: Channel # = (channelStatusGroupNumber * 8)
Bit 6: Channel # = (channelStatusGroupNumber * 8) - 1
Bit 5: Channel # = (channelStatusGroupNumber * 8) - 2
Bit 4: Channel # = (channelStatusGroupNumber * 8) - 3
Bit 3: Channel # = (channelStatusGroupNumber * 8) - 4
Bit 2: Channel # = (channelStatusGroupNumber * 8) - 5
Bit 1: Channel # = (channelStatusGroupNumber * 8) - 6
Bit 0: Channel # = (channelStatusGroupNumber * 8) - 7
<Object Identifier> 1.3.6.1.4.1.1206.4.2.1.8.4.1.3
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   0
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the channelStatusGroupYellows data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param channelStatusGroupYellows_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
channelStatusGroupYellows_get( channelStatusGroupTable_rowreq_ctx *rowreq_ctx, long * channelStatusGroupYellows_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != channelStatusGroupYellows_val_ptr );


    DEBUGMSGTL(("verbose:channelStatusGroupTable:channelStatusGroupYellows_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the channelStatusGroupYellows data.
 * copy (* channelStatusGroupYellows_val_ptr ) from rowreq_ctx->data
 */
    (* channelStatusGroupYellows_val_ptr ) = rowreq_ctx->data.channelStatusGroupYellows;

    return MFD_SUCCESS;
} /* channelStatusGroupYellows_get */

/*---------------------------------------------------------------------
 * NTCIP1202-v03::channelStatusGroupEntry.channelStatusGroupGreens
 * channelStatusGroupGreens is subid 4 of channelStatusGroupEntry.
 * Its status is Mandatory, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.1206.4.2.1.8.4.1.4
 * Description:
<Definition> Channel Green Output Status Mask, when a
bit=1, the Channel Green is currently active. When a bit=0, the
Channel Green is NOT currently active.
Bit 7: Channel # = (channelStatusGroupNumber * 8)
Bit 6: Channel # = (channelStatusGroupNumber * 8) - 1
Bit 5: Channel # = (channelStatusGroupNumber * 8) - 2
Bit 4: Channel # = (channelStatusGroupNumber * 8) - 3
Bit 3: Channel # = (channelStatusGroupNumber * 8) - 4
Bit 2: Channel # = (channelStatusGroupNumber * 8) - 5
Bit 1: Channel # = (channelStatusGroupNumber * 8) - 6
Bit 0: Channel # = (channelStatusGroupNumber * 8) - 7
<Object Identifier> 1.3.6.1.4.1.1206.4.2.1.8.4.1.4
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   0
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the channelStatusGroupGreens data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param channelStatusGroupGreens_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
channelStatusGroupGreens_get( channelStatusGroupTable_rowreq_ctx *rowreq_ctx, long * channelStatusGroupGreens_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != channelStatusGroupGreens_val_ptr );


    DEBUGMSGTL(("verbose:channelStatusGroupTable:channelStatusGroupGreens_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the channelStatusGroupGreens data.
 * copy (* channelStatusGroupGreens_val_ptr ) from rowreq_ctx->data
 */
    (* channelStatusGroupGreens_val_ptr ) = rowreq_ctx->data.channelStatusGroupGreens;

    return MFD_SUCCESS;
} /* channelStatusGroupGreens_get */



/** @} */
