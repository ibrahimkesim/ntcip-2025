/*
 * Note: this file originally auto-generated by mib2c
 * using mib2c.scalar.conf
 */

#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>
#include "channel_data_1202.h"

/** Initializes the channel module */
void
init_channel_1202(void)
{
    const oid maxChannels_oid[] = { 1,3,6,1,4,1,1206,4,2,1,8,1 };
    const oid maxChannelStatusGroups_oid[] = { 1,3,6,1,4,1,1206,4,2,1,8,3 };

  DEBUGMSGTL(("channel", "Initializing\n"));

    netsnmp_register_scalar(
        netsnmp_create_handler_registration("maxChannels", handle_maxChannels,
                               maxChannels_oid, OID_LENGTH(maxChannels_oid),
                               HANDLER_CAN_RONLY
        ));
    netsnmp_register_scalar(
        netsnmp_create_handler_registration("maxChannelStatusGroups", handle_maxChannelStatusGroups,
                               maxChannelStatusGroups_oid, OID_LENGTH(maxChannelStatusGroups_oid),
                               HANDLER_CAN_RONLY
        ));
}

int
handle_maxChannels(netsnmp_mib_handler *handler,
                          netsnmp_handler_registration *reginfo,
                          netsnmp_agent_request_info   *reqinfo,
                          netsnmp_request_info         *requests)
{
    /* We are never called for a GETNEXT if it's registered as a
       "instance", as it's "magically" handled for us.  */

    /* a instance handler also only hands us one request at a time, so
       we don't need to loop over a list of requests; we'll only get one. */
    
    switch(reqinfo->mode) {

        case MODE_GET:
            snmp_set_var_typed_value(requests->requestvb, ASN_INTEGER,
                                     get_maxChannelStatusGroups(),
                                     sizeof(int));
            break;


        default:
            /* we should never get here, so this is a really bad error */
            snmp_log(LOG_ERR, "unknown mode (%d) in handle_maxChannels\n", reqinfo->mode );
            return SNMP_ERR_GENERR;
    }

    return SNMP_ERR_NOERROR;
}
int
handle_maxChannelStatusGroups(netsnmp_mib_handler *handler,
                          netsnmp_handler_registration *reginfo,
                          netsnmp_agent_request_info   *reqinfo,
                          netsnmp_request_info         *requests)
{
    /* We are never called for a GETNEXT if it's registered as a
       "instance", as it's "magically" handled for us.  */

    /* a instance handler also only hands us one request at a time, so
       we don't need to loop over a list of requests; we'll only get one. */
    
    switch(reqinfo->mode) {

        case MODE_GET:
            snmp_set_var_typed_value(requests->requestvb, ASN_INTEGER,
                                     get_maxChannelStatusGroups(),
                                     sizeof(int));
            break;


        default:
            /* we should never get here, so this is a really bad error */
            snmp_log(LOG_ERR, "unknown mode (%d) in handle_maxChannelStatusGroups\n", reqinfo->mode );
            return SNMP_ERR_GENERR;
    }

    return SNMP_ERR_NOERROR;
}
