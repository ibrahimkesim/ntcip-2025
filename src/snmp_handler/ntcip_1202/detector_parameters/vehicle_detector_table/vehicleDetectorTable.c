/*
 * Note: this file originally auto-generated by mib2c
 * using mfd-top.m2c
 */
/** \page MFD helper for vehicleDetectorTable
 *
 * \section intro Introduction
 * Introductory text.
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "vehicleDetectorTable.h"

#include <net-snmp/agent/mib_modules.h>

#include "vehicleDetectorTable_interface.h"
#include "log.h"

const oid vehicleDetectorTable_oid[] = { VEHICLEDETECTORTABLE_OID };
const int vehicleDetectorTable_oid_size = OID_LENGTH(vehicleDetectorTable_oid);

    vehicleDetectorTable_registration  vehicleDetectorTable_user_context;

void initialize_table_vehicleDetectorTable(void);
void shutdown_table_vehicleDetectorTable(void);


/**
 * Initializes the vehicleDetectorTable module
 */
void
init_vehicleDetectorTable(void)
{
    DEBUGMSGTL(("verbose:vehicleDetectorTable:init_vehicleDetectorTable","called\n"));
    //log_debug("vehiclededectortable.c  init_vehicleDetectorTable- 40.satır");
    /*
     * TODO:300:o: Perform vehicleDetectorTable one-time module initialization.
     */
     
    /*
     * here we initialize all the tables we're planning on supporting
     */
    if (should_init("vehicleDetectorTable"))
        initialize_table_vehicleDetectorTable();

} /* init_vehicleDetectorTable */

/**
 * Shut-down the vehicleDetectorTable module (agent is exiting)
 */
void
shutdown_vehicleDetectorTable(void)
{
    if (should_init("vehicleDetectorTable"))
        shutdown_table_vehicleDetectorTable();

}

/**
 * Initialize the table vehicleDetectorTable 
 *    (Define its contents and how it's structured)
 */
void
initialize_table_vehicleDetectorTable(void)
{
    vehicleDetectorTable_registration * user_context;
    u_long flags;
    //log_debug("\nvehıcleDetectorTable_73\n");
    DEBUGMSGTL(("verbose:vehicleDetectorTable:initialize_table_vehicleDetectorTable","called\n"));

    /*
     * TODO:301:o: Perform vehicleDetectorTable one-time table initialization.
     */

    /*
     * TODO:302:o: |->Initialize vehicleDetectorTable user context
     * if you'd like to pass in a pointer to some data for this
     * table, allocate or set it up here.
     */
    /*
     * a netsnmp_data_list is a simple way to store void pointers. A simple
     * string token is used to add, find or remove pointers.
     */
    user_context = netsnmp_create_data_list("vehicleDetectorTable", NULL, NULL);
    
    /*
     * No support for any flags yet, but in the future you would
     * set any flags here.
     */
    flags = 0;
    
    /*
     * call interface initialization code
     */
    _vehicleDetectorTable_initialize_interface(user_context, flags);
} /* initialize_table_vehicleDetectorTable */

/**
 * Shutdown the table vehicleDetectorTable 
 */
void
shutdown_table_vehicleDetectorTable(void)
{
    /*
     * call interface shutdown code
     */
    _vehicleDetectorTable_shutdown_interface(&vehicleDetectorTable_user_context);
}

/**
 * extra context initialization (eg default values)
 *
 * @param rowreq_ctx    : row request context
 * @param user_init_ctx : void pointer for user (parameter to rowreq_ctx_allocate)
 *
 * @retval MFD_SUCCESS  : no errors
 * @retval MFD_ERROR    : error (context allocate will fail)
 */
int
vehicleDetectorTable_rowreq_ctx_init(vehicleDetectorTable_rowreq_ctx *rowreq_ctx,
                           void *user_init_ctx)
{
    DEBUGMSGTL(("verbose:vehicleDetectorTable:vehicleDetectorTable_rowreq_ctx_init","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    
    /*
     * TODO:210:o: |-> Perform extra vehicleDetectorTable rowreq initialization. (eg DEFVALS)
     */
    rowreq_ctx->data.vehicleDetectorPairedDetector = 0;
    
    rowreq_ctx->data.vehicleDetectorPairedDetectorSpacing = 0;
    
    rowreq_ctx->data.vehicleDetectorLength = 65535;
    
    rowreq_ctx->data.vehicleDetectorTravelMode = VEHICLEDETECTORTRAVELMODE_VEHICLE;
    

    return MFD_SUCCESS;
} /* vehicleDetectorTable_rowreq_ctx_init */

/**
 * extra context cleanup
 *
 */
void vehicleDetectorTable_rowreq_ctx_cleanup(vehicleDetectorTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:vehicleDetectorTable:vehicleDetectorTable_rowreq_ctx_cleanup","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    
    /*
     * TODO:211:o: |-> Perform extra vehicleDetectorTable rowreq cleanup.
     */
} /* vehicleDetectorTable_rowreq_ctx_cleanup */

/************************************************************
 * the *_should_save routine is called to determine if a row
 * should be stored persistently.
 *
 * Note that this is not a 'dirty' check (i.e. if a row has changed),
 * but a check for volatile rows that should not be saved between
 * restarts.
 *
 * return 1 if the row should be stored
 * return 0 if the row should not be stored
 */
int
vehicleDetectorTable_container_should_save(vehicleDetectorTable_rowreq_ctx * rowreq_ctx)
{
    
    return 1; /* save the row */
}

/**
 * pre-request callback
 *
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_ERROR                : other error
 */
int
vehicleDetectorTable_pre_request(vehicleDetectorTable_registration * user_context)
{
    DEBUGMSGTL(("verbose:vehicleDetectorTable:vehicleDetectorTable_pre_request","called\n"));

    /*
     * TODO:510:o: Perform vehicleDetectorTable pre-request actions.
     */

    return MFD_SUCCESS;
} /* vehicleDetectorTable_pre_request */

/**
 * post-request callback
 *
 * Note:
 *   New rows have been inserted into the container, and
 *   deleted rows have been removed from the container and
 *   released.
 *
 * @param user_context
 * @param rc : MFD_SUCCESS if all requests succeeded
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : other error (ignored)
 */
int
vehicleDetectorTable_post_request(vehicleDetectorTable_registration * user_context, int rc)
{
    DEBUGMSGTL(("verbose:vehicleDetectorTable:vehicleDetectorTable_post_request","called\n"));

    /*
     * TODO:511:o: Perform vehicleDetectorTable post-request actions.
     */

    /*
     * check to set if any rows were changed.
     */
    if (vehicleDetectorTable_dirty_get()) {
        /*
         * check if request was successful. If so, this would be
         * a good place to save data to its persistent store.
         */
        if (MFD_SUCCESS == rc) {
            /*
             * save changed rows, if you haven't already
             */
            snmp_store(netsnmp_ds_get_string(NETSNMP_DS_LIBRARY_ID,
                                             NETSNMP_DS_LIB_APPTYPE));
        }
        
        vehicleDetectorTable_dirty_set(0); /* clear table dirty flag */
    }

    return MFD_SUCCESS;
} /* vehicleDetectorTable_post_request */


/** @{ */
