/*
 * Note: this file originally auto-generated by mib2c
 * using mfd-data-set.m2c
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "vehicleDetectorControlGroupTable.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table vehicleDetectorControlGroupTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * NTCIP1202-v03::vehicleDetectorControlGroupTable is subid 12 of detector.
 * Its status is Mandatory.
 * OID: .1.3.6.1.4.1.1206.4.2.1.2.12, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that the undo context has been allocated with
 * vehicleDetectorControlGroupTable_allocate_data(), but may need extra
 * initialization similar to what you may have done in
 * vehicleDetectorControlGroupTable_rowreq_ctx_init().
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (vehicleDetectorControlGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
vehicleDetectorControlGroupTable_undo_setup( vehicleDetectorControlGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:vehicleDetectorControlGroupTable:vehicleDetectorControlGroupTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup vehicleDetectorControlGroupTable undo.
     * set up vehicleDetectorControlGroupTable undo information, in preparation for a set.
     * Undo storage is in (* vehicleDetectorControlGroupActuation_val_ptr )*
     */

    return rc;
} /* vehicleDetectorControlGroupTable_undo_setup */

/**
 * Undo a set request.
 *
 * This function will be called before the individual node undo
 * functions are called. If you need to do any undo that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo function will only be called
 * if that node is being set to a new value.
 *
 * If there is anything  specific to a particular column (e.g. releasing
 * memory for a string), you should do that setup in the node's undo
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (vehicleDetectorControlGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
vehicleDetectorControlGroupTable_undo( vehicleDetectorControlGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:vehicleDetectorControlGroupTable:vehicleDetectorControlGroupTable_undo","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> vehicleDetectorControlGroupTable undo.
     * vehicleDetectorControlGroupTable undo information, in response to a failed set.
     * Undo storage is in (* vehicleDetectorControlGroupActuation_val_ptr )*
     */

    return rc;
} /* vehicleDetectorControlGroupTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (vehicleDetectorControlGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
vehicleDetectorControlGroupTable_undo_cleanup( vehicleDetectorControlGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:vehicleDetectorControlGroupTable:vehicleDetectorControlGroupTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup vehicleDetectorControlGroupTable undo.
     * Undo storage is in (* vehicleDetectorControlGroupActuation_val_ptr )*
     */

    return rc;
} /* vehicleDetectorControlGroupTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * vehicleDetectorControlGroupTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param vehicleDetectorControlGroupTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
vehicleDetectorControlGroupTable_commit( vehicleDetectorControlGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:vehicleDetectorControlGroupTable:vehicleDetectorControlGroupTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit vehicleDetectorControlGroupTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & COLUMN_VEHICLEDETECTORCONTROLGROUPACTUATION_FLAG) {
       save_flags &= ~COLUMN_VEHICLEDETECTORCONTROLGROUPACTUATION_FLAG; /* clear vehicleDetectorControlGroupActuation */
       /*
        * TODO:482:o: |-> commit column vehicleDetectorControlGroupActuation.
        */
       rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"vehicleDetectorControlGroupTable column vehicleDetectorControlGroupActuation commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo vehicleDetectorControlGroupActuation
             */
            rowreq_ctx->column_set_flags |= COLUMN_VEHICLEDETECTORCONTROLGROUPACTUATION_FLAG;
       }
    }

    /*
     * if we successfully commited this row, set the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags |= MFD_ROW_DIRTY;
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    //return rc;
    rc = MFD_SUCCESS; // semih

} /* vehicleDetectorControlGroupTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * vehicleDetectorControlGroupTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param vehicleDetectorControlGroupTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
vehicleDetectorControlGroupTable_undo_commit( vehicleDetectorControlGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:vehicleDetectorControlGroupTable:vehicleDetectorControlGroupTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo vehicleDetectorControlGroupTable commit.
     * check each column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & COLUMN_VEHICLEDETECTORCONTROLGROUPACTUATION_FLAG) {}
     */

    
    /*
     * if we successfully un-commited this row, clear the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags &= ~MFD_ROW_DIRTY;
    }

    return rc;
} /* vehicleDetectorControlGroupTable_undo_commit */

/*
 * TODO:440:M: Implement vehicleDetectorControlGroupTable node value checks.
 * TODO:450:M: Implement vehicleDetectorControlGroupTable undo functions.
 * TODO:460:M: Implement vehicleDetectorControlGroupTable set functions.
 * TODO:480:M: Implement vehicleDetectorControlGroupTable commit functions.
 */
/*---------------------------------------------------------------------
 * NTCIP1202-v03::vehicleDetectorControlGroupEntry.vehicleDetectorControlGroupActuation
 * vehicleDetectorControlGroupActuation is subid 2 of vehicleDetectorControlGroupEntry.
 * Its status is Mandatory, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.1206.4.2.1.2.12.1.2
 * Description:
<Definition> This object is used to allow a remote entity
to place an actuation on a vehicle detector. When a bit = 1, an
actuation is placed on the vehicle detector. When a bit = 0, no
actuation is placed on the vehicle detector. An NTCIP actuation
is placed using this object and is treated the same as an
external actuation, so all detector functions are still
applicable, including delay, extension, diagnostics, and report
objects, such as vehicleDetectorStatusGroupActive and
volumeOccupancyReport.
Bit 7: Det # = ( vehicleDetectorControlGroupNumber * 8)
Bit 6: Det # = ( vehicleDetectorControlGroupNumber * 8) - 1
Bit 5: Det # = ( vehicleDetectorControlGroupNumber * 8) - 2
Bit 4: Det # = ( vehicleDetectorControlGroupNumber * 8) - 3
Bit 3: Det # = ( vehicleDetectorControlGroupNumber * 8) - 4
Bit 2: Det # = ( vehicleDetectorControlGroupNumber * 8) - 5
Bit 1: Det # = ( vehicleDetectorControlGroupNumber * 8) - 6
Bit 0: Det # = ( vehicleDetectorControlGroupNumber * 8) � 7
The device shall reset this object to ZERO when in BACKUP Mode. A
write to this object shall reset the Backup timer to ZERO (see
unitBackupTime).
<Object Identifier> 1.3.6.1.4.1.1206.4.2.1.2.12.1.2
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param vehicleDetectorControlGroupActuation_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * vehicleDetectorControlGroupTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
vehicleDetectorControlGroupActuation_check_value( vehicleDetectorControlGroupTable_rowreq_ctx *rowreq_ctx, long vehicleDetectorControlGroupActuation_val)
{
    DEBUGMSGTL(("verbose:vehicleDetectorControlGroupTable:vehicleDetectorControlGroupActuation_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid vehicleDetectorControlGroupActuation value.
     */

    return MFD_SUCCESS; /* vehicleDetectorControlGroupActuation value not illegal */
} /* vehicleDetectorControlGroupActuation_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (vehicleDetectorControlGroupTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * vehicleDetectorControlGroupTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
vehicleDetectorControlGroupActuation_undo_setup( vehicleDetectorControlGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:vehicleDetectorControlGroupTable:vehicleDetectorControlGroupActuation_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup vehicleDetectorControlGroupActuation undo.
     */
    /*
     * copy vehicleDetectorControlGroupActuation data
     * set rowreq_ctx->undo->vehicleDetectorControlGroupActuation from rowreq_ctx->data.vehicleDetectorControlGroupActuation
     */
    rowreq_ctx->undo->vehicleDetectorControlGroupActuation = rowreq_ctx->data.vehicleDetectorControlGroupActuation;


    return MFD_SUCCESS;
} /* vehicleDetectorControlGroupActuation_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param vehicleDetectorControlGroupActuation_val
 *        A long containing the new value.
 */
int
vehicleDetectorControlGroupActuation_set( vehicleDetectorControlGroupTable_rowreq_ctx *rowreq_ctx, long vehicleDetectorControlGroupActuation_val )
{

    DEBUGMSGTL(("verbose:vehicleDetectorControlGroupTable:vehicleDetectorControlGroupActuation_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set vehicleDetectorControlGroupActuation value.
     * set vehicleDetectorControlGroupActuation value in rowreq_ctx->data
     */
    rowreq_ctx->data.vehicleDetectorControlGroupActuation = vehicleDetectorControlGroupActuation_val;

    return MFD_SUCCESS;
} /* vehicleDetectorControlGroupActuation_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
vehicleDetectorControlGroupActuation_undo( vehicleDetectorControlGroupTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:vehicleDetectorControlGroupTable:vehicleDetectorControlGroupActuation_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up vehicleDetectorControlGroupActuation undo.
     */
    /*
     * copy vehicleDetectorControlGroupActuation data
     * set rowreq_ctx->data.vehicleDetectorControlGroupActuation from rowreq_ctx->undo->vehicleDetectorControlGroupActuation
     */
    rowreq_ctx->data.vehicleDetectorControlGroupActuation = rowreq_ctx->undo->vehicleDetectorControlGroupActuation;

    
    return MFD_SUCCESS;
} /* vehicleDetectorControlGroupActuation_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * vehicleDetectorControlGroupTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-vehicleDetectorControlGroupTable if you don't have dependencies)
 */
int
vehicleDetectorControlGroupTable_check_dependencies(vehicleDetectorControlGroupTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:vehicleDetectorControlGroupTable:vehicleDetectorControlGroupTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check vehicleDetectorControlGroupTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    return rc;
} /* vehicleDetectorControlGroupTable_check_dependencies */

/** @} */
