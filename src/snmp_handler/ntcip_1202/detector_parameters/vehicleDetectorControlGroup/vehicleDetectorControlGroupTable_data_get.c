/*
 * Note: this file originally auto-generated by mib2c
 * using mfd-data-get.m2c
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "vehicleDetectorControlGroupTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement vehicleDetectorControlGroupTable get routines.
 * TODO:240:M: Implement vehicleDetectorControlGroupTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table vehicleDetectorControlGroupTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * NTCIP1202-v03::vehicleDetectorControlGroupTable is subid 12 of detector.
 * Its status is Mandatory.
 * OID: .1.3.6.1.4.1.1206.4.2.1.2.12, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement vehicleDetectorControlGroupTable data context functions.
 */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 * @param vehicleDetectorControlGroupNumber_val
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
vehicleDetectorControlGroupTable_indexes_set_tbl_idx(vehicleDetectorControlGroupTable_mib_index *tbl_idx, long vehicleDetectorControlGroupNumber_val)
{
    DEBUGMSGTL(("verbose:vehicleDetectorControlGroupTable:vehicleDetectorControlGroupTable_indexes_set_tbl_idx","called\n"));

    /* vehicleDetectorControlGroupNumber(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/R/d/h */
    tbl_idx->vehicleDetectorControlGroupNumber = vehicleDetectorControlGroupNumber_val;
    

    return MFD_SUCCESS;
} /* vehicleDetectorControlGroupTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
vehicleDetectorControlGroupTable_indexes_set(vehicleDetectorControlGroupTable_rowreq_ctx *rowreq_ctx, long vehicleDetectorControlGroupNumber_val)
{
    DEBUGMSGTL(("verbose:vehicleDetectorControlGroupTable:vehicleDetectorControlGroupTable_indexes_set","called\n"));

    if(MFD_SUCCESS != vehicleDetectorControlGroupTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , vehicleDetectorControlGroupNumber_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != vehicleDetectorControlGroupTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* vehicleDetectorControlGroupTable_indexes_set */


/*---------------------------------------------------------------------
 * NTCIP1202-v03::vehicleDetectorControlGroupEntry.vehicleDetectorControlGroupActuation
 * vehicleDetectorControlGroupActuation is subid 2 of vehicleDetectorControlGroupEntry.
 * Its status is Mandatory, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.1206.4.2.1.2.12.1.2
 * Description:
<Definition> This object is used to allow a remote entity
to place an actuation on a vehicle detector. When a bit = 1, an
actuation is placed on the vehicle detector. When a bit = 0, no
actuation is placed on the vehicle detector. An NTCIP actuation
is placed using this object and is treated the same as an
external actuation, so all detector functions are still
applicable, including delay, extension, diagnostics, and report
objects, such as vehicleDetectorStatusGroupActive and
volumeOccupancyReport.
Bit 7: Det # = ( vehicleDetectorControlGroupNumber * 8)
Bit 6: Det # = ( vehicleDetectorControlGroupNumber * 8) - 1
Bit 5: Det # = ( vehicleDetectorControlGroupNumber * 8) - 2
Bit 4: Det # = ( vehicleDetectorControlGroupNumber * 8) - 3
Bit 3: Det # = ( vehicleDetectorControlGroupNumber * 8) - 4
Bit 2: Det # = ( vehicleDetectorControlGroupNumber * 8) - 5
Bit 1: Det # = ( vehicleDetectorControlGroupNumber * 8) - 6
Bit 0: Det # = ( vehicleDetectorControlGroupNumber * 8) � 7
The device shall reset this object to ZERO when in BACKUP Mode. A
write to this object shall reset the Backup timer to ZERO (see
unitBackupTime).
<Object Identifier> 1.3.6.1.4.1.1206.4.2.1.2.12.1.2
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the vehicleDetectorControlGroupActuation data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param vehicleDetectorControlGroupActuation_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
vehicleDetectorControlGroupActuation_get( vehicleDetectorControlGroupTable_rowreq_ctx *rowreq_ctx, long * vehicleDetectorControlGroupActuation_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != vehicleDetectorControlGroupActuation_val_ptr );


    DEBUGMSGTL(("verbose:vehicleDetectorControlGroupTable:vehicleDetectorControlGroupActuation_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the vehicleDetectorControlGroupActuation data.
 * copy (* vehicleDetectorControlGroupActuation_val_ptr ) from rowreq_ctx->data
 */
    (* vehicleDetectorControlGroupActuation_val_ptr ) = rowreq_ctx->data.vehicleDetectorControlGroupActuation;

    return MFD_SUCCESS;
} /* vehicleDetectorControlGroupActuation_get */

int
vehicleDetectorControlGroupNumber_get(vehicleDetectorControlGroupTable_rowreq_ctx *rowreq_ctx, long *vehicleDetectorControlGroupNumber_val_ptr)
{
    netsnmp_assert(NULL != vehicleDetectorControlGroupNumber_val_ptr);
    netsnmp_assert(NULL != rowreq_ctx);

    DEBUGMSGTL(("verbose:vehicleDetectorControlGroupTable:vehicleDetectorControlGroupNumber_get", "called\n"));

    (*vehicleDetectorControlGroupNumber_val_ptr) = rowreq_ctx->tbl_idx.vehicleDetectorControlGroupNumber;

    return MFD_SUCCESS;
}


/** @} */
