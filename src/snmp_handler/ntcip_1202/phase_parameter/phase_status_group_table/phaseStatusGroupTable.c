/*
 * Note: this file originally auto-generated by mib2c
 * using mfd-top.m2c
 */
/** \page MFD helper for phaseStatusGroupTable
 *
 * \section intro Introduction
 * Introductory text.
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "phaseStatusGroupTable.h"

#include <net-snmp/agent/mib_modules.h>

#include "phaseStatusGroupTable_interface.h"

const oid phaseStatusGroupTable_oid[] = { PHASESTATUSGROUPTABLE_OID };
const int phaseStatusGroupTable_oid_size = OID_LENGTH(phaseStatusGroupTable_oid);

    phaseStatusGroupTable_registration  phaseStatusGroupTable_user_context;

void initialize_table_phaseStatusGroupTable(void);
void shutdown_table_phaseStatusGroupTable(void);


/**
 * Initializes the phaseStatusGroupTable module
 */
void
init_phaseStatusGroupTable(void)
{
    DEBUGMSGTL(("verbose:phaseStatusGroupTable:init_phaseStatusGroupTable","called\n"));

    /*
     * TODO:300:o: Perform phaseStatusGroupTable one-time module initialization.
     */
     
    /*
     * here we initialize all the tables we're planning on supporting
     */
    if (should_init("phaseStatusGroupTable"))
        initialize_table_phaseStatusGroupTable();

} /* init_phaseStatusGroupTable */

/**
 * Shut-down the phaseStatusGroupTable module (agent is exiting)
 */
void
shutdown_phaseStatusGroupTable(void)
{
    if (should_init("phaseStatusGroupTable"))
        shutdown_table_phaseStatusGroupTable();

}

/**
 * Initialize the table phaseStatusGroupTable 
 *    (Define its contents and how it's structured)
 */
void
initialize_table_phaseStatusGroupTable(void)
{
    phaseStatusGroupTable_registration * user_context;
    u_long flags;

    DEBUGMSGTL(("verbose:phaseStatusGroupTable:initialize_table_phaseStatusGroupTable","called\n"));

    /*
     * TODO:301:o: Perform phaseStatusGroupTable one-time table initialization.
     */

    /*
     * TODO:302:o: |->Initialize phaseStatusGroupTable user context
     * if you'd like to pass in a pointer to some data for this
     * table, allocate or set it up here.
     */
    /*
     * a netsnmp_data_list is a simple way to store void pointers. A simple
     * string token is used to add, find or remove pointers.
     */
    user_context = netsnmp_create_data_list("phaseStatusGroupTable", NULL, NULL);
    
    /*
     * No support for any flags yet, but in the future you would
     * set any flags here.
     */
    flags = 0;
    
    /*
     * call interface initialization code
     */
    _phaseStatusGroupTable_initialize_interface(user_context, flags);
} /* initialize_table_phaseStatusGroupTable */

/**
 * Shutdown the table phaseStatusGroupTable 
 */
void
shutdown_table_phaseStatusGroupTable(void)
{
    /*
     * call interface shutdown code
     */
    _phaseStatusGroupTable_shutdown_interface(&phaseStatusGroupTable_user_context);
}

/**
 * extra context initialization (eg default values)
 *
 * @param rowreq_ctx    : row request context
 * @param user_init_ctx : void pointer for user (parameter to rowreq_ctx_allocate)
 *
 * @retval MFD_SUCCESS  : no errors
 * @retval MFD_ERROR    : error (context allocate will fail)
 */
int
phaseStatusGroupTable_rowreq_ctx_init(phaseStatusGroupTable_rowreq_ctx *rowreq_ctx,
                           void *user_init_ctx)
{
    DEBUGMSGTL(("verbose:phaseStatusGroupTable:phaseStatusGroupTable_rowreq_ctx_init","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    
    /*
     * TODO:210:o: |-> Perform extra phaseStatusGroupTable rowreq initialization. (eg DEFVALS)
     */

    return MFD_SUCCESS;
} /* phaseStatusGroupTable_rowreq_ctx_init */

/**
 * extra context cleanup
 *
 */
void phaseStatusGroupTable_rowreq_ctx_cleanup(phaseStatusGroupTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:phaseStatusGroupTable:phaseStatusGroupTable_rowreq_ctx_cleanup","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    
    /*
     * TODO:211:o: |-> Perform extra phaseStatusGroupTable rowreq cleanup.
     */
} /* phaseStatusGroupTable_rowreq_ctx_cleanup */

/**
 * pre-request callback
 *
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_ERROR                : other error
 */
int
phaseStatusGroupTable_pre_request(phaseStatusGroupTable_registration * user_context)
{
    DEBUGMSGTL(("verbose:phaseStatusGroupTable:phaseStatusGroupTable_pre_request","called\n"));

    /*
     * TODO:510:o: Perform phaseStatusGroupTable pre-request actions.
     */

    return MFD_SUCCESS;
} /* phaseStatusGroupTable_pre_request */

/**
 * post-request callback
 *
 * Note:
 *   New rows have been inserted into the container, and
 *   deleted rows have been removed from the container and
 *   released.
 *
 * @param user_context
 * @param rc : MFD_SUCCESS if all requests succeeded
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : other error (ignored)
 */
int
phaseStatusGroupTable_post_request(phaseStatusGroupTable_registration * user_context, int rc)
{
    DEBUGMSGTL(("verbose:phaseStatusGroupTable:phaseStatusGroupTable_post_request","called\n"));

    /*
     * TODO:511:o: Perform phaseStatusGroupTable post-request actions.
     */

    return MFD_SUCCESS;
} /* phaseStatusGroupTable_post_request */


/** @{ */
