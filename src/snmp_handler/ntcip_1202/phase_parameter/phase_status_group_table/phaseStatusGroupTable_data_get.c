/*
 * Note: this file originally auto-generated by mib2c
 * using mfd-data-get.m2c
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "phaseStatusGroupTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement phaseStatusGroupTable get routines.
 * TODO:240:M: Implement phaseStatusGroupTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table phaseStatusGroupTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * NTCIP1202-v03::phaseStatusGroupTable is subid 4 of phase.
 * Its status is Mandatory.
 * OID: .1.3.6.1.4.1.1206.4.2.1.1.4, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement phaseStatusGroupTable data context functions.
 */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 * @param phaseStatusGroupNumber_val
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
phaseStatusGroupTable_indexes_set_tbl_idx(phaseStatusGroupTable_mib_index *tbl_idx, long phaseStatusGroupNumber_val)
{
    DEBUGMSGTL(("verbose:phaseStatusGroupTable:phaseStatusGroupTable_indexes_set_tbl_idx","called\n"));

    /* phaseStatusGroupNumber(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/R/d/h */
    tbl_idx->phaseStatusGroupNumber = phaseStatusGroupNumber_val;
    

    return MFD_SUCCESS;
} /* phaseStatusGroupTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
phaseStatusGroupTable_indexes_set(phaseStatusGroupTable_rowreq_ctx *rowreq_ctx, long phaseStatusGroupNumber_val)
{
    DEBUGMSGTL(("verbose:phaseStatusGroupTable:phaseStatusGroupTable_indexes_set","called\n"));

    if(MFD_SUCCESS != phaseStatusGroupTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , phaseStatusGroupNumber_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != phaseStatusGroupTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* phaseStatusGroupTable_indexes_set */


/*---------------------------------------------------------------------
 * NTCIP1202-v03::phaseStatusGroupEntry.phaseStatusGroupReds
 * phaseStatusGroupReds is subid 2 of phaseStatusGroupEntry.
 * Its status is Mandatory, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.1206.4.2.1.1.4.1.2
 * Description:
<Definition> Phase Red Output Status Mask, when a bit = 1,
the Phase Red is currently active. When a bit = 0, the Phase Red
is NOT currently active.
Bit 7: Phase # = (phaseStatusGroupNumber * 8)
 Bit 6: Phase # = (phaseStatusGroupNumber * 8) - 1
 Bit 5: Phase # = (phaseStatusGroupNumber * 8) - 2
 Bit 4: Phase # = (phaseStatusGroupNumber * 8) - 3
 Bit 3: Phase # = (phaseStatusGroupNumber * 8) - 4
 Bit 2: Phase # = (phaseStatusGroupNumber * 8) - 5
 Bit 1: Phase # = (phaseStatusGroupNumber * 8) - 6
 Bit 0: Phase # = (phaseStatusGroupNumber * 8) - 7

<Object Identifier> 1.3.6.1.4.1.1206.4.2.1.1.4.1.2
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   0
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the phaseStatusGroupReds data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param phaseStatusGroupReds_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
phaseStatusGroupReds_get( phaseStatusGroupTable_rowreq_ctx *rowreq_ctx, long * phaseStatusGroupReds_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != phaseStatusGroupReds_val_ptr );


    DEBUGMSGTL(("verbose:phaseStatusGroupTable:phaseStatusGroupReds_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the phaseStatusGroupReds data.
 * copy (* phaseStatusGroupReds_val_ptr ) from rowreq_ctx->data
 */
    (* phaseStatusGroupReds_val_ptr ) = rowreq_ctx->data.phaseStatusGroupReds;

    return MFD_SUCCESS;
} /* phaseStatusGroupReds_get */

/*---------------------------------------------------------------------
 * NTCIP1202-v03::phaseStatusGroupEntry.phaseStatusGroupYellows
 * phaseStatusGroupYellows is subid 3 of phaseStatusGroupEntry.
 * Its status is Mandatory, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.1206.4.2.1.1.4.1.3
 * Description:
<Definition> Phase Yellow Output Status Mask, when a bit =
1, the Phase Yellow is currently active. When a  bit = 0, the
Phase Yellow is NOT currently active.
 Bit 7: Phase # = (phaseStatusGroupNumber * 8)
 Bit 6: Phase # = (phaseStatusGroupNumber * 8) - 1
 Bit 5: Phase # = (phaseStatusGroupNumber * 8) - 2
 Bit 4: Phase # = (phaseStatusGroupNumber * 8) - 3
 Bit 3: Phase # = (phaseStatusGroupNumber * 8) - 4
 Bit 2: Phase # = (phaseStatusGroupNumber * 8) - 5
 Bit 1: Phase # = (phaseStatusGroupNumber * 8) - 6
 Bit 0: Phase # = (phaseStatusGroupNumber * 8) - 7
<Object Identifier> 1.3.6.1.4.1.1206.4.2.1.1.4.1.3
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   0
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the phaseStatusGroupYellows data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param phaseStatusGroupYellows_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
phaseStatusGroupYellows_get( phaseStatusGroupTable_rowreq_ctx *rowreq_ctx, long * phaseStatusGroupYellows_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != phaseStatusGroupYellows_val_ptr );


    DEBUGMSGTL(("verbose:phaseStatusGroupTable:phaseStatusGroupYellows_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the phaseStatusGroupYellows data.
 * copy (* phaseStatusGroupYellows_val_ptr ) from rowreq_ctx->data
 */
    (* phaseStatusGroupYellows_val_ptr ) = rowreq_ctx->data.phaseStatusGroupYellows;

    return MFD_SUCCESS;
} /* phaseStatusGroupYellows_get */

/*---------------------------------------------------------------------
 * NTCIP1202-v03::phaseStatusGroupEntry.phaseStatusGroupGreens
 * phaseStatusGroupGreens is subid 4 of phaseStatusGroupEntry.
 * Its status is Mandatory, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.1206.4.2.1.1.4.1.4
 * Description:
<Definition> Phase Green Output Status Mask, when a bit =
1, the Phase Green is currently active. When a bit = 0, the Phase
Green is NOT currently active.
 Bit 7: Phase # = (phaseStatusGroupNumber * 8)
 Bit 6: Phase # = (phaseStatusGroupNumber * 8) - 1
 Bit 5: Phase # = (phaseStatusGroupNumber * 8) - 2
 Bit 4: Phase # = (phaseStatusGroupNumber * 8) - 3
 Bit 3: Phase # = (phaseStatusGroupNumber * 8) - 4
 Bit 2: Phase # = (phaseStatusGroupNumber * 8) - 5
 Bit 1: Phase # = (phaseStatusGroupNumber * 8) - 6
 Bit 0: Phase # = (phaseStatusGroupNumber * 8) - 7
<Object Identifier> 1.3.6.1.4.1.1206.4.2.1.1.4.1.4
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   0
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the phaseStatusGroupGreens data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param phaseStatusGroupGreens_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
phaseStatusGroupGreens_get( phaseStatusGroupTable_rowreq_ctx *rowreq_ctx, long * phaseStatusGroupGreens_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != phaseStatusGroupGreens_val_ptr );


    DEBUGMSGTL(("verbose:phaseStatusGroupTable:phaseStatusGroupGreens_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the phaseStatusGroupGreens data.
 * copy (* phaseStatusGroupGreens_val_ptr ) from rowreq_ctx->data
 */
    (* phaseStatusGroupGreens_val_ptr ) = rowreq_ctx->data.phaseStatusGroupGreens;

    return MFD_SUCCESS;
} /* phaseStatusGroupGreens_get */

/*---------------------------------------------------------------------
 * NTCIP1202-v03::phaseStatusGroupEntry.phaseStatusGroupDontWalks
 * phaseStatusGroupDontWalks is subid 5 of phaseStatusGroupEntry.
 * Its status is Mandatory, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.1206.4.2.1.1.4.1.5
 * Description:
<Definition> Phase Dont Walk Output Status Mask, when a
bit = 1, the Phase Dont Walk is currently active. When a bit = 0,
the Phase Dont Walk is NOT currently active.
 Bit 7: Phase # = (phaseStatusGroupNumber * 8)
 Bit 6: Phase # = (phaseStatusGroupNumber * 8) - 1
 Bit 5: Phase # = (phaseStatusGroupNumber * 8) - 2
 Bit 4: Phase # = (phaseStatusGroupNumber * 8) - 3
 Bit 3: Phase # = (phaseStatusGroupNumber * 8) - 4
 Bit 2: Phase # = (phaseStatusGroupNumber * 8) - 5
 Bit 1: Phase # = (phaseStatusGroupNumber * 8) - 6
 Bit 0: Phase # = (phaseStatusGroupNumber * 8) - 7
<Object Identifier> 1.3.6.1.4.1.1206.4.2.1.1.4.1.5
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   0
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the phaseStatusGroupDontWalks data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param phaseStatusGroupDontWalks_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
phaseStatusGroupDontWalks_get( phaseStatusGroupTable_rowreq_ctx *rowreq_ctx, long * phaseStatusGroupDontWalks_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != phaseStatusGroupDontWalks_val_ptr );


    DEBUGMSGTL(("verbose:phaseStatusGroupTable:phaseStatusGroupDontWalks_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the phaseStatusGroupDontWalks data.
 * copy (* phaseStatusGroupDontWalks_val_ptr ) from rowreq_ctx->data
 */
    (* phaseStatusGroupDontWalks_val_ptr ) = rowreq_ctx->data.phaseStatusGroupDontWalks;

    return MFD_SUCCESS;
} /* phaseStatusGroupDontWalks_get */

/*---------------------------------------------------------------------
 * NTCIP1202-v03::phaseStatusGroupEntry.phaseStatusGroupPedClears
 * phaseStatusGroupPedClears is subid 6 of phaseStatusGroupEntry.
 * Its status is Mandatory, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.1206.4.2.1.1.4.1.6
 * Description:
<Definition> Phase Ped Clear Output Status Mask, when a
bit = 1, the Phase Ped Clear is currently active. When a bit = 0,
the Phase Ped Clear is NOT currently active.
 Bit 7: Phase # = (phaseStatusGroupNumber * 8)
 Bit 6: Phase # = (phaseStatusGroupNumber * 8) - 1
 Bit 5: Phase # = (phaseStatusGroupNumber * 8) - 2
 Bit 4: Phase # = (phaseStatusGroupNumber * 8) - 3
 Bit 3: Phase # = (phaseStatusGroupNumber * 8) - 4
 Bit 2: Phase # = (phaseStatusGroupNumber * 8) - 5
 Bit 1: Phase # = (phaseStatusGroupNumber * 8) - 6
 Bit 0: Phase # = (phaseStatusGroupNumber * 8) - 7
<Object Identifier> 1.3.6.1.4.1.1206.4.2.1.1.4.1.6
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   0
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the phaseStatusGroupPedClears data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param phaseStatusGroupPedClears_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
phaseStatusGroupPedClears_get( phaseStatusGroupTable_rowreq_ctx *rowreq_ctx, long * phaseStatusGroupPedClears_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != phaseStatusGroupPedClears_val_ptr );


    DEBUGMSGTL(("verbose:phaseStatusGroupTable:phaseStatusGroupPedClears_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the phaseStatusGroupPedClears data.
 * copy (* phaseStatusGroupPedClears_val_ptr ) from rowreq_ctx->data
 */
    (* phaseStatusGroupPedClears_val_ptr ) = rowreq_ctx->data.phaseStatusGroupPedClears;

    return MFD_SUCCESS;
} /* phaseStatusGroupPedClears_get */

/*---------------------------------------------------------------------
 * NTCIP1202-v03::phaseStatusGroupEntry.phaseStatusGroupWalks
 * phaseStatusGroupWalks is subid 7 of phaseStatusGroupEntry.
 * Its status is Mandatory, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.1206.4.2.1.1.4.1.7
 * Description:
<Definition> Phase Walk Output Status Mask, when a bit =
1, the Phase Walk is currently active. When a bit = 0, the Phase
Walk is NOT currently active.
 Bit 7: Phase # = (phaseStatusGroupNumber * 8)
 Bit 6: Phase # = (phaseStatusGroupNumber * 8) - 1
 Bit 5: Phase # = (phaseStatusGroupNumber * 8) - 2
 Bit 4: Phase # = (phaseStatusGroupNumber * 8) - 3
 Bit 3: Phase # = (phaseStatusGroupNumber * 8) - 4
 Bit 2: Phase # = (phaseStatusGroupNumber * 8) - 5
 Bit 1: Phase # = (phaseStatusGroupNumber * 8) - 6
 Bit 0: Phase # = (phaseStatusGroupNumber * 8) - 7
<Object Identifier> 1.3.6.1.4.1.1206.4.2.1.1.4.1.7
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   0
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the phaseStatusGroupWalks data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param phaseStatusGroupWalks_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
phaseStatusGroupWalks_get( phaseStatusGroupTable_rowreq_ctx *rowreq_ctx, long * phaseStatusGroupWalks_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != phaseStatusGroupWalks_val_ptr );


    DEBUGMSGTL(("verbose:phaseStatusGroupTable:phaseStatusGroupWalks_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the phaseStatusGroupWalks data.
 * copy (* phaseStatusGroupWalks_val_ptr ) from rowreq_ctx->data
 */
    (* phaseStatusGroupWalks_val_ptr ) = rowreq_ctx->data.phaseStatusGroupWalks;

    return MFD_SUCCESS;
} /* phaseStatusGroupWalks_get */

/*---------------------------------------------------------------------
 * NTCIP1202-v03::phaseStatusGroupEntry.phaseStatusGroupVehCalls
 * phaseStatusGroupVehCalls is subid 8 of phaseStatusGroupEntry.
 * Its status is Mandatory, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.1206.4.2.1.1.4.1.8
 * Description:
<Definition> Phase Vehicle Call Status Mask, when a bit =
1, the Phase vehicle currently has a call for service. When a bit

= 0, the Phase vehicle currently does NOT have a call for
service.
 Bit 7: Phase # = (phaseStatusGroupNumber * 8)
 Bit 6: Phase # = (phaseStatusGroupNumber * 8) - 1
 Bit 5: Phase # = (phaseStatusGroupNumber * 8) - 2
 Bit 4: Phase # = (phaseStatusGroupNumber * 8) - 3
 Bit 3: Phase # = (phaseStatusGroupNumber * 8) - 4
 Bit 2: Phase # = (phaseStatusGroupNumber * 8) - 5
 Bit 1: Phase # = (phaseStatusGroupNumber * 8) - 6
 Bit 0: Phase # = (phaseStatusGroupNumber * 8) - 7
<Object Identifier> 1.3.6.1.4.1.1206.4.2.1.1.4.1.8
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   0
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the phaseStatusGroupVehCalls data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param phaseStatusGroupVehCalls_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
phaseStatusGroupVehCalls_get( phaseStatusGroupTable_rowreq_ctx *rowreq_ctx, long * phaseStatusGroupVehCalls_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != phaseStatusGroupVehCalls_val_ptr );


    DEBUGMSGTL(("verbose:phaseStatusGroupTable:phaseStatusGroupVehCalls_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the phaseStatusGroupVehCalls data.
 * copy (* phaseStatusGroupVehCalls_val_ptr ) from rowreq_ctx->data
 */
    (* phaseStatusGroupVehCalls_val_ptr ) = rowreq_ctx->data.phaseStatusGroupVehCalls;

    return MFD_SUCCESS;
} /* phaseStatusGroupVehCalls_get */

/*---------------------------------------------------------------------
 * NTCIP1202-v03::phaseStatusGroupEntry.phaseStatusGroupPedCalls
 * phaseStatusGroupPedCalls is subid 9 of phaseStatusGroupEntry.
 * Its status is Mandatory, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.1206.4.2.1.1.4.1.9
 * Description:
<Definition> Phase Pedestrian Call Status Mask, when a bit
= 1, the  Phase pedestrian currently has a call for service. When
a bit = 0, the Phase pedestrian currently does NOT have a call
for service.
 Bit 7: Phase # = (phaseStatusGroupNumber * 8)
 Bit 6: Phase # = (phaseStatusGroupNumber * 8) - 1
 Bit 5: Phase # = (phaseStatusGroupNumber * 8) - 2
 Bit 4: Phase # = (phaseStatusGroupNumber * 8) - 3
 Bit 3: Phase # = (phaseStatusGroupNumber * 8) - 4
 Bit 2: Phase # = (phaseStatusGroupNumber * 8) - 5
 Bit 1: Phase # = (phaseStatusGroupNumber * 8) - 6
 Bit 0: Phase # = (phaseStatusGroupNumber * 8) - 7
<Object Identifier> 1.3.6.1.4.1.1206.4.2.1.1.4.1.9
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   0
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the phaseStatusGroupPedCalls data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param phaseStatusGroupPedCalls_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
phaseStatusGroupPedCalls_get( phaseStatusGroupTable_rowreq_ctx *rowreq_ctx, long * phaseStatusGroupPedCalls_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != phaseStatusGroupPedCalls_val_ptr );


    DEBUGMSGTL(("verbose:phaseStatusGroupTable:phaseStatusGroupPedCalls_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the phaseStatusGroupPedCalls data.
 * copy (* phaseStatusGroupPedCalls_val_ptr ) from rowreq_ctx->data
 */
    (* phaseStatusGroupPedCalls_val_ptr ) = rowreq_ctx->data.phaseStatusGroupPedCalls;

    return MFD_SUCCESS;
} /* phaseStatusGroupPedCalls_get */

/*---------------------------------------------------------------------
 * NTCIP1202-v03::phaseStatusGroupEntry.phaseStatusGroupPhaseOns
 * phaseStatusGroupPhaseOns is subid 10 of phaseStatusGroupEntry.
 * Its status is Mandatory, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.1206.4.2.1.1.4.1.10
 * Description:
<Definition> Phase On Status Mask, when a bit = 1, the
Phase is currently active. When a bit = 0, the Phase currently is
NOT active. The phase is ON during the Green, Yellow, & Red
Clearance intervals of that phase. It shall be permissible for
this STATUS to be True (bit=1) during the Red Dwell state.
 Bit 7: Phase # = (phaseStatusGroupNumber * 8)
 Bit 6: Phase # = (phaseStatusGroupNumber * 8) - 1
 Bit 5: Phase # = (phaseStatusGroupNumber * 8) - 2
 Bit 4: Phase # = (phaseStatusGroupNumber * 8) - 3
 Bit 3: Phase # = (phaseStatusGroupNumber * 8) - 4
 Bit 2: Phase # = (phaseStatusGroupNumber * 8) - 5
 Bit 1: Phase # = (phaseStatusGroupNumber * 8) - 6
 Bit 0: Phase # = (phaseStatusGroupNumber * 8) - 7
<Object Identifier> 1.3.6.1.4.1.1206.4.2.1.1.4.1.10
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   0
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the phaseStatusGroupPhaseOns data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param phaseStatusGroupPhaseOns_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
phaseStatusGroupPhaseOns_get( phaseStatusGroupTable_rowreq_ctx *rowreq_ctx, long * phaseStatusGroupPhaseOns_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != phaseStatusGroupPhaseOns_val_ptr );


    DEBUGMSGTL(("verbose:phaseStatusGroupTable:phaseStatusGroupPhaseOns_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the phaseStatusGroupPhaseOns data.
 * copy (* phaseStatusGroupPhaseOns_val_ptr ) from rowreq_ctx->data
 */
    (* phaseStatusGroupPhaseOns_val_ptr ) = rowreq_ctx->data.phaseStatusGroupPhaseOns;

    return MFD_SUCCESS;
} /* phaseStatusGroupPhaseOns_get */

/*---------------------------------------------------------------------
 * NTCIP1202-v03::phaseStatusGroupEntry.phaseStatusGroupPhaseNexts
 * phaseStatusGroupPhaseNexts is subid 11 of phaseStatusGroupEntry.
 * Its status is Mandatory, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.1206.4.2.1.1.4.1.11
 * Description:
<Definition> Phase Next Status Mask, when a bit = 1, the
Phase currently is committed to be NEXT in sequence & remains
present until the phase becomes active (On/Timing). When a bit =
0, the Phase currently is NOT committed to be NEXT in sequence.
The phase next to be serviced shall be determined at the end of
the green interval of the terminating phase; except that if the
decision cannot be made at the end of the Green interval, it
shall not be made until after the end of all Vehicle Change &
Clearance intervals.
 Bit 7: Phase # = (phaseStatusGroupNumber * 8)
 Bit 6: Phase # = (phaseStatusGroupNumber * 8) - 1
 Bit 5: Phase # = (phaseStatusGroupNumber * 8) - 2
 Bit 4: Phase # = (phaseStatusGroupNumber * 8) - 3
 Bit 3: Phase # = (phaseStatusGroupNumber * 8) - 4
 Bit 2: Phase # = (phaseStatusGroupNumber * 8) - 5
 Bit 1: Phase # = (phaseStatusGroupNumber * 8) - 6
 Bit 0: Phase # = (phaseStatusGroupNumber * 8) - 7
<Object Identifier> 1.3.6.1.4.1.1206.4.2.1.1.4.1.11
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   0
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the phaseStatusGroupPhaseNexts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param phaseStatusGroupPhaseNexts_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
phaseStatusGroupPhaseNexts_get( phaseStatusGroupTable_rowreq_ctx *rowreq_ctx, long * phaseStatusGroupPhaseNexts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != phaseStatusGroupPhaseNexts_val_ptr );


    DEBUGMSGTL(("verbose:phaseStatusGroupTable:phaseStatusGroupPhaseNexts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the phaseStatusGroupPhaseNexts data.
 * copy (* phaseStatusGroupPhaseNexts_val_ptr ) from rowreq_ctx->data
 */
    (* phaseStatusGroupPhaseNexts_val_ptr ) = rowreq_ctx->data.phaseStatusGroupPhaseNexts;

    return MFD_SUCCESS;
} /* phaseStatusGroupPhaseNexts_get */



/** @} */
