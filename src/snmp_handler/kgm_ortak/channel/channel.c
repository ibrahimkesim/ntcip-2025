/*
 * Note: this file originally auto-generated by mib2c
 * using mib2c.scalar.conf
 */

#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>
#include "utils.h"
#include "channel_data.h"

/** Initializes the channel module */
void
init_channel(void)
{
    const oid maxGeneralFaultStateGroups_oid[] = { 1,3,6,1,4,1,59873,4,2,1,2,5 };
    const oid maxTrafficLightFaultStatusGroups_oid[] = { 1,3,6,1,4,1,59873,4,2,1,2,1 };
    const oid maxChannelErrorFlagsGroups_oid[] = { 1,3,6,1,4,1,59873,4,2,1,2,3 };

  DEBUGMSGTL(("channel", "Initializing\n"));

    netsnmp_register_scalar(
        netsnmp_create_handler_registration("maxGeneralFaultStateGroups", handle_maxGeneralFaultStateGroups,
                               maxGeneralFaultStateGroups_oid, OID_LENGTH(maxGeneralFaultStateGroups_oid),
                               HANDLER_CAN_RONLY
        ));
    netsnmp_register_scalar(
        netsnmp_create_handler_registration("maxTrafficLightFaultStatusGroups", handle_maxTrafficLightFaultStatusGroups,
                               maxTrafficLightFaultStatusGroups_oid, OID_LENGTH(maxTrafficLightFaultStatusGroups_oid),
                               HANDLER_CAN_RONLY
        ));
    netsnmp_register_scalar(
        netsnmp_create_handler_registration("maxChannelErrorFlagsGroups", handle_maxChannelErrorFlagsGroups,
                               maxChannelErrorFlagsGroups_oid, OID_LENGTH(maxChannelErrorFlagsGroups_oid),
                               HANDLER_CAN_RONLY
        ));
}

int
handle_maxGeneralFaultStateGroups(netsnmp_mib_handler *handler,
                          netsnmp_handler_registration *reginfo,
                          netsnmp_agent_request_info   *reqinfo,
                          netsnmp_request_info         *requests)
{
    /* We are never called for a GETNEXT if it's registered as a
       "instance", as it's "magically" handled for us.  */

    /* a instance handler also only hands us one request at a time, so
       we don't need to loop over a list of requests; we'll only get one. */
    
    switch(reqinfo->mode) {

        case MODE_GET:
            snmp_set_var_typed_value(requests->requestvb, ASN_INTEGER,
                                     get_maxGeneralFaultStateGroups(),
                                     sizeof(int));
            break;


        default:
            /* we should never get here, so this is a really bad error */
            snmp_log(LOG_ERR, "unknown mode (%d) in handle_maxGeneralFaultStateGroups\n", reqinfo->mode );
            return SNMP_ERR_GENERR;
    }

    return SNMP_ERR_NOERROR;
}
int
handle_maxTrafficLightFaultStatusGroups(netsnmp_mib_handler *handler,
                          netsnmp_handler_registration *reginfo,
                          netsnmp_agent_request_info   *reqinfo,
                          netsnmp_request_info         *requests)
{
    /* We are never called for a GETNEXT if it's registered as a
       "instance", as it's "magically" handled for us.  */

    /* a instance handler also only hands us one request at a time, so
       we don't need to loop over a list of requests; we'll only get one. */
    
    switch(reqinfo->mode) {

        case MODE_GET:
            snmp_set_var_typed_value(requests->requestvb, ASN_INTEGER,
                                     get_maxTrafficLightFaultStatusGroups(),
                                     sizeof(int));
            break;


        default:
            /* we should never get here, so this is a really bad error */
            snmp_log(LOG_ERR, "unknown mode (%d) in handle_maxTrafficLightFaultStatusGroups\n", reqinfo->mode );
            return SNMP_ERR_GENERR;
    }

    return SNMP_ERR_NOERROR;
}
int
handle_maxChannelErrorFlagsGroups(netsnmp_mib_handler *handler,
                          netsnmp_handler_registration *reginfo,
                          netsnmp_agent_request_info   *reqinfo,
                          netsnmp_request_info         *requests)
{
    /* We are never called for a GETNEXT if it's registered as a
       "instance", as it's "magically" handled for us.  */

    /* a instance handler also only hands us one request at a time, so
       we don't need to loop over a list of requests; we'll only get one. */
    
    switch(reqinfo->mode) {

        case MODE_GET:
            snmp_set_var_typed_value(requests->requestvb, ASN_INTEGER,
                                     get_maxChannelErrorFlagsGroups(),
                                     sizeof(int));
            break;


        default:
            /* we should never get here, so this is a really bad error */
            snmp_log(LOG_ERR, "unknown mode (%d) in handle_maxChannelErrorFlagsGroups\n", reqinfo->mode );
            return SNMP_ERR_GENERR;
    }

    return SNMP_ERR_NOERROR;
}
