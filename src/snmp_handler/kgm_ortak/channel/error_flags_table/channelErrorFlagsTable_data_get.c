/*
 * Note: this file originally auto-generated by mib2c
 * using mfd-data-get.m2c
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "channelErrorFlagsTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement channelErrorFlagsTable get routines.
 * TODO:240:M: Implement channelErrorFlagsTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table channelErrorFlagsTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * kgm-ortak::channelErrorFlagsTable is subid 4 of channel.
 * Its status is Mandatory.
 * OID: .1.3.6.1.4.1.59873.4.2.1.2.4, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement channelErrorFlagsTable data context functions.
 */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 * @param errorgroupNumber_val
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
channelErrorFlagsTable_indexes_set_tbl_idx(channelErrorFlagsTable_mib_index *tbl_idx, long errorgroupNumber_val)
{
    DEBUGMSGTL(("verbose:channelErrorFlagsTable:channelErrorFlagsTable_indexes_set_tbl_idx","called\n"));

    /* errorgroupNumber(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/R/d/h */
    tbl_idx->errorgroupNumber = errorgroupNumber_val;
    

    return MFD_SUCCESS;
} /* channelErrorFlagsTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
channelErrorFlagsTable_indexes_set(channelErrorFlagsTable_rowreq_ctx *rowreq_ctx, long errorgroupNumber_val)
{
    DEBUGMSGTL(("verbose:channelErrorFlagsTable:channelErrorFlagsTable_indexes_set","called\n"));

    if(MFD_SUCCESS != channelErrorFlagsTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , errorgroupNumber_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != channelErrorFlagsTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* channelErrorFlagsTable_indexes_set */


/*---------------------------------------------------------------------
 * kgm-ortak::channelErrorFlagsEntry.redLampAllFailure
 * redLampAllFailure is subid 2 of channelErrorFlagsEntry.
 * Its status is Mandatory, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.59873.4.2.1.2.4.1.2
 * Description:
<Definition>
Kanal Kırmızı arıza durumları
Bit=1 ise ilgili kanal arızası var.
Bit=0 ise ilgili kanal arızası yok.
Bit 7: Channel # = (errorgroupNumber * 8)
Bit 6: Channel # = (errorgroupNumber * 8) - 1
Bit 5: Channel # = (errorgroupNumber * 8) - 2
Bit 4: Channel # = (errorgroupNumber * 8) - 3
Bit 3: Channel # = (errorgroupNumber * 8) - 4
Bit 2: Channel # = (errorgroupNumber * 8) - 5
Bit 1: Channel # = (errorgroupNumber * 8) - 6
Bit 0: Channel # = (errorgroupNumber * 8) – 7

<Object Identifier> KGM OID
<Unit>  
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   0
 *
 * Ranges:  1 - 65535;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the redLampAllFailure data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param redLampAllFailure_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
redLampAllFailure_get( channelErrorFlagsTable_rowreq_ctx *rowreq_ctx, long * redLampAllFailure_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != redLampAllFailure_val_ptr );


    DEBUGMSGTL(("verbose:channelErrorFlagsTable:redLampAllFailure_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the redLampAllFailure data.
 * copy (* redLampAllFailure_val_ptr ) from rowreq_ctx->data
 */
    (* redLampAllFailure_val_ptr ) = rowreq_ctx->data.redLampAllFailure;

    return MFD_SUCCESS;
} /* redLampAllFailure_get */

/*---------------------------------------------------------------------
 * kgm-ortak::channelErrorFlagsEntry.greenLampAllFailure
 * greenLampAllFailure is subid 3 of channelErrorFlagsEntry.
 * Its status is Mandatory, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.59873.4.2.1.2.4.1.3
 * Description:
<Definition>
Kanal Yeşil arıza durumları
Bit=1 ise ilgili kanal arızası var.
Bit=0 ise ilgili kanal arızası yok.
Bit 7: Channel # = (errorgroupNumber * 8)
Bit 6: Channel # = (errorgroupNumber * 8) - 1
Bit 5: Channel # = (errorgroupNumber * 8) - 2
Bit 4: Channel # = (errorgroupNumber * 8) - 3
Bit 3: Channel # = (errorgroupNumber * 8) - 4
Bit 2: Channel # = (errorgroupNumber * 8) - 5
Bit 1: Channel # = (errorgroupNumber * 8) - 6
Bit 0: Channel # = (errorgroupNumber * 8) – 7

<Object Identifier> KGM OID
<Unit>  
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   0
 *
 * Ranges:  1 - 65535;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the greenLampAllFailure data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param greenLampAllFailure_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
greenLampAllFailure_get( channelErrorFlagsTable_rowreq_ctx *rowreq_ctx, long * greenLampAllFailure_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != greenLampAllFailure_val_ptr );


    DEBUGMSGTL(("verbose:channelErrorFlagsTable:greenLampAllFailure_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the greenLampAllFailure data.
 * copy (* greenLampAllFailure_val_ptr ) from rowreq_ctx->data
 */
    (* greenLampAllFailure_val_ptr ) = rowreq_ctx->data.greenLampAllFailure;

    return MFD_SUCCESS;
} /* greenLampAllFailure_get */

/*---------------------------------------------------------------------
 * kgm-ortak::channelErrorFlagsEntry.yellowLampAllFailure
 * yellowLampAllFailure is subid 4 of channelErrorFlagsEntry.
 * Its status is Mandatory, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.59873.4.2.1.2.4.1.4
 * Description:
<Definition>
Kanal Sarı arıza durumları
Bit=1 ise ilgili kanal arızası var.
Bit=0 ise ilgili kanal arızası yok.
Bit 7: Channel # = (errorgroupNumber * 8)
Bit 6: Channel # = (errorgroupNumber * 8) - 1
Bit 5: Channel # = (errorgroupNumber * 8) - 2
Bit 4: Channel # = (errorgroupNumber * 8) - 3
Bit 3: Channel # = (errorgroupNumber * 8) - 4
Bit 2: Channel # = (errorgroupNumber * 8) - 5
Bit 1: Channel # = (errorgroupNumber * 8) - 6
Bit 0: Channel # = (errorgroupNumber * 8) – 7

<Object Identifier> KGM OID
<Unit>  
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   0
 *
 * Ranges:  1 - 65535;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the yellowLampAllFailure data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param yellowLampAllFailure_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
yellowLampAllFailure_get( channelErrorFlagsTable_rowreq_ctx *rowreq_ctx, long * yellowLampAllFailure_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != yellowLampAllFailure_val_ptr );


    DEBUGMSGTL(("verbose:channelErrorFlagsTable:yellowLampAllFailure_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the yellowLampAllFailure data.
 * copy (* yellowLampAllFailure_val_ptr ) from rowreq_ctx->data
 */
    (* yellowLampAllFailure_val_ptr ) = rowreq_ctx->data.yellowLampAllFailure;

    return MFD_SUCCESS;
} /* yellowLampAllFailure_get */

/*---------------------------------------------------------------------
 * kgm-ortak::channelErrorFlagsEntry.undesiredRedSignal
 * undesiredRedSignal is subid 5 of channelErrorFlagsEntry.
 * Its status is Mandatory, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.59873.4.2.1.2.4.1.5
 * Description:
<Definition>
Kanal İstemsiz kırmızı sinyal durumları
Bit=1 ise ilgili kanalda istemsiz sinyal vardır.
Bit=0 ise ilgili kanalda istemsiz sinyal yoktur.
Bit 7: Channel # = (errorgroupNumber * 8)
Bit 6: Channel # = (errorgroupNumber * 8) - 1
Bit 5: Channel # = (errorgroupNumber * 8) - 2
Bit 4: Channel # = (errorgroupNumber * 8) - 3
Bit 3: Channel # = (errorgroupNumber * 8) - 4
Bit 2: Channel # = (errorgroupNumber * 8) - 5
Bit 1: Channel # = (errorgroupNumber * 8) - 6
Bit 0: Channel # = (errorgroupNumber * 8) – 7

<Object Identifier> KGM OID
<Unit>  
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   0
 *
 * Ranges:  1 - 65535;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the undesiredRedSignal data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param undesiredRedSignal_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
undesiredRedSignal_get( channelErrorFlagsTable_rowreq_ctx *rowreq_ctx, long * undesiredRedSignal_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != undesiredRedSignal_val_ptr );


    DEBUGMSGTL(("verbose:channelErrorFlagsTable:undesiredRedSignal_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the undesiredRedSignal data.
 * copy (* undesiredRedSignal_val_ptr ) from rowreq_ctx->data
 */
    (* undesiredRedSignal_val_ptr ) = rowreq_ctx->data.undesiredRedSignal;

    return MFD_SUCCESS;
} /* undesiredRedSignal_get */

/*---------------------------------------------------------------------
 * kgm-ortak::channelErrorFlagsEntry.undesiredGreenSignal
 * undesiredGreenSignal is subid 6 of channelErrorFlagsEntry.
 * Its status is Mandatory, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.59873.4.2.1.2.4.1.6
 * Description:
<Definition>
Kanal İstemsiz yeşil sinyal durumları
Bit=1 ise ilgili kanalda istemsiz sinyal vardır.
Bit=0 ise ilgili kanalda istemsiz sinyal yoktur.
Bit 7: Channel # = (errorgroupNumber * 8)
Bit 6: Channel # = (errorgroupNumber * 8) - 1
Bit 5: Channel # = (errorgroupNumber * 8) - 2
Bit 4: Channel # = (errorgroupNumber * 8) - 3
Bit 3: Channel # = (errorgroupNumber * 8) - 4
Bit 2: Channel # = (errorgroupNumber * 8) - 5
Bit 1: Channel # = (errorgroupNumber * 8) - 6
Bit 0: Channel # = (errorgroupNumber * 8) – 7

<Object Identifier> KGM OID
<Unit>  
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   0
 *
 * Ranges:  1 - 65535;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the undesiredGreenSignal data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param undesiredGreenSignal_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
undesiredGreenSignal_get( channelErrorFlagsTable_rowreq_ctx *rowreq_ctx, long * undesiredGreenSignal_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != undesiredGreenSignal_val_ptr );


    DEBUGMSGTL(("verbose:channelErrorFlagsTable:undesiredGreenSignal_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the undesiredGreenSignal data.
 * copy (* undesiredGreenSignal_val_ptr ) from rowreq_ctx->data
 */
    (* undesiredGreenSignal_val_ptr ) = rowreq_ctx->data.undesiredGreenSignal;

    return MFD_SUCCESS;
} /* undesiredGreenSignal_get */

/*---------------------------------------------------------------------
 * kgm-ortak::channelErrorFlagsEntry.undesiredYellowSignal
 * undesiredYellowSignal is subid 7 of channelErrorFlagsEntry.
 * Its status is Mandatory, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.59873.4.2.1.2.4.1.7
 * Description:
<Definition>
Kanal İstemsiz sarı sinyal durumları
Bit=1 ise ilgili kanalda istemsiz sinyal vardır.
Bit=0 ise ilgili kanalda istemsiz sinyal yoktur.
Bit 7: Channel # = (errorgroupNumber * 8)
Bit 6: Channel # = (errorgroupNumber * 8) - 1
Bit 5: Channel # = (errorgroupNumber * 8) - 2
Bit 4: Channel # = (errorgroupNumber * 8) - 3
Bit 3: Channel # = (errorgroupNumber * 8) - 4
Bit 2: Channel # = (errorgroupNumber * 8) - 5
Bit 1: Channel # = (errorgroupNumber * 8) - 6
Bit 0: Channel # = (errorgroupNumber * 8) – 7

<Object Identifier> KGM OID
<Unit>  
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   0
 *
 * Ranges:  1 - 65535;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the undesiredYellowSignal data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param undesiredYellowSignal_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
undesiredYellowSignal_get( channelErrorFlagsTable_rowreq_ctx *rowreq_ctx, long * undesiredYellowSignal_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != undesiredYellowSignal_val_ptr );


    DEBUGMSGTL(("verbose:channelErrorFlagsTable:undesiredYellowSignal_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the undesiredYellowSignal data.
 * copy (* undesiredYellowSignal_val_ptr ) from rowreq_ctx->data
 */
    (* undesiredYellowSignal_val_ptr ) = rowreq_ctx->data.undesiredYellowSignal;

    return MFD_SUCCESS;
} /* undesiredYellowSignal_get */



/** @} */
