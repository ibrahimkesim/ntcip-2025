/*
 * Note: this file originally auto-generated by mib2c
 * using mfd-data-access.m2c
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "utils.h"
#include "channel_data.h"


#include "ChannelGeneralFaultStateTable_data_access.h"

/** @ingroup interface
 * @addtogroup data_access data_access: Routines to access data
 *
 * These routines are used to locate the data used to satisfy
 * requests.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table ChannelGeneralFaultStateTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * kgm-ortak::ChannelGeneralFaultStateTable is subid 12 of channel.
 * Its status is Mandatory.
 * OID: .1.3.6.1.4.1.40019.4.2.1.2.12, length: 12
*/

/**
 * initialization for ChannelGeneralFaultStateTable data access
 *
 * This function is called during startup to allow you to
 * allocate any resources you need for the data table.
 *
 * @param ChannelGeneralFaultStateTable_reg
 *        Pointer to ChannelGeneralFaultStateTable_registration
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : unrecoverable error.
 */
int
ChannelGeneralFaultStateTable_init_data(ChannelGeneralFaultStateTable_registration * ChannelGeneralFaultStateTable_reg)
{
    DEBUGMSGTL(("verbose:ChannelGeneralFaultStateTable:ChannelGeneralFaultStateTable_init_data","called\n"));

    static unsigned int gen_fault_state_table_columns[] = {
        COLUMN_CHANNELFAULTSTATUSGROUPNUMBER,
        COLUMN_CHANNELFAULTS
    };

    static netsnmp_column_info valid_columns;
    valid_columns.isRange = 0;
    valid_columns.details.list = gen_fault_state_table_columns;
    valid_columns.list_count = sizeof(gen_fault_state_table_columns)/sizeof(unsigned int);
    ChannelGeneralFaultStateTable_valid_columns_set(&valid_columns);

    return MFD_SUCCESS;
} /* ChannelGeneralFaultStateTable_init_data */

/**
 * container overview
 *
 */

/**
 * container initialization
 *
 * @param container_ptr_ptr A pointer to a container pointer. If you
 *        create a custom container, use this parameter to return it
 *        to the MFD helper. If set to NULL, the MFD helper will
 *        allocate a container for you.
 * @param  cache A pointer to a cache structure. You can set the timeout
 *         and other cache flags using this pointer.
 *
 *  This function is called at startup to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases. If no custom
 *  container is allocated, the MFD code will create one for your.
 *
 *  This is also the place to set up cache behavior. The default, to
 *  simply set the cache timeout, will work well with the default
 *  container. If you are using a custom container, you may want to
 *  look at the cache helper documentation to see if there are any
 *  flags you want to set.
 *
 * @remark
 *  This would also be a good place to do any initialization needed
 *  for you data source. For example, opening a connection to another
 *  process that will supply the data, opening a database, etc.
 */
void
ChannelGeneralFaultStateTable_container_init(netsnmp_container **container_ptr_ptr,
                             netsnmp_cache *cache)
{
    DEBUGMSGTL(("verbose:ChannelGeneralFaultStateTable:ChannelGeneralFaultStateTable_container_init","called\n"));
    
    if (NULL == container_ptr_ptr) {
        snmp_log(LOG_ERR,"bad container param to ChannelGeneralFaultStateTable_container_init\n");
        return;
    }

    /*
     * For advanced users, you can use a custom container. If you
     * do not create one, one will be created for you.
     */
    *container_ptr_ptr = NULL;

    if (NULL == cache) {
        snmp_log(LOG_ERR,"bad cache param to ChannelGeneralFaultStateTable_container_init\n");
        return;
    }

    /*
     * TODO:345:A: Set up ChannelGeneralFaultStateTable cache properties.
     *
     * Also for advanced users, you can set parameters for the
     * cache. Do not change the magic pointer, as it is used
     * by the MFD helper. To completely disable caching, set
     * cache->enabled to 0.
     */
    cache->timeout = CHANNELGENERALFAULTSTATETABLE_CACHE_TIMEOUT; /* seconds */
} /* ChannelGeneralFaultStateTable_container_init */

/**
 * container shutdown
 *
 * @param container_ptr A pointer to the container.
 *
 *  This function is called at shutdown to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases.
 *
 *  This function is called before ChannelGeneralFaultStateTable_container_free().
 *
 * @remark
 *  This would also be a good place to do any cleanup needed
 *  for you data source. For example, closing a connection to another
 *  process that supplied the data, closing a database, etc.
 */
void
ChannelGeneralFaultStateTable_container_shutdown(netsnmp_container *container_ptr)
{
    DEBUGMSGTL(("verbose:ChannelGeneralFaultStateTable:ChannelGeneralFaultStateTable_container_shutdown","called\n"));
    
    if (NULL == container_ptr) {
        snmp_log(LOG_ERR,"bad params to ChannelGeneralFaultStateTable_container_shutdown\n");
        return;
    }

} /* ChannelGeneralFaultStateTable_container_shutdown */

/**
 * load initial data
 *
 * TODO:350:M: Implement ChannelGeneralFaultStateTable data load
 * This function will also be called by the cache helper to load
 * the container again (after the container free function has been
 * called to free the previous contents).
 *
 * @param container container to which items should be inserted
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_RESOURCE_UNAVAILABLE : Can't access data source
 * @retval MFD_ERROR                : other error.
 *
 *  This function is called to load the index(es) (and data, optionally)
 *  for the every row in the data set.
 *
 * @remark
 *  While loading the data, the only important thing is the indexes.
 *  If access to your data is cheap/fast (e.g. you have a pointer to a
 *  structure in memory), it would make sense to update the data here.
 *  If, however, the accessing the data involves more work (e.g. parsing
 *  some other existing data, or peforming calculations to derive the data),
 *  then you can limit yourself to setting the indexes and saving any
 *  information you will need later. Then use the saved information in
 *  ChannelGeneralFaultStateTable_row_prep() for populating data.
 *
 * @note
 *  If you need consistency between rows (like you want statistics
 *  for each row to be from the same time frame), you should set all
 *  data here.
 *
 */
int
ChannelGeneralFaultStateTable_container_load(netsnmp_container *container)
{
    ChannelGeneralFaultStateTable_rowreq_ctx *rowreq_ctx;
    size_t                 count = 0;

    /*
     * storage for each column
     */
    long   channelFaultStatusGroupNumber;

    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    ChannelGeneralFaultStateTable_rowreq_ctx* table_ptr = NULL;

    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/
    /*
     * TODO:351:M: |-> Load/update data in the ChannelGeneralFaultStateTable container.
     * loop over your ChannelGeneralFaultStateTable data, allocate a rowreq context,
     * set the index(es) [and data, optionally] and insert into
     * the container.
     */
    while( 1 ) {
        /*
        ***************************************************
        ***             START EXAMPLE CODE              ***
        ***---------------------------------------------***/
        table_ptr = get_channel_general_fault_state_table(count);
        /*
        * check for end of data
        */
        if(NULL == table_ptr)
            break;

        /*
        * parse line into variables
        */
        channelFaultStatusGroupNumber = table_ptr->tbl_idx.channelFaultStatusGroupNumber;
        /*
        ***---------------------------------------------***
        ***              END  EXAMPLE CODE              ***
        ***************************************************/

        /*
        * TODO:352:M: |   |-> set indexes in new ChannelGeneralFaultStateTable rowreq context.
        * data context will be set from the param (unless NULL,
        *      in which case a new data context will be allocated)
        */
        rowreq_ctx = ChannelGeneralFaultStateTable_allocate_rowreq_ctx(NULL);
        if (NULL == rowreq_ctx) {
            snmp_log(LOG_ERR, "memory allocation failed\n");
            return MFD_RESOURCE_UNAVAILABLE;
        }
        if(MFD_SUCCESS != ChannelGeneralFaultStateTable_indexes_set(rowreq_ctx
                            , channelFaultStatusGroupNumber
            )) {
            snmp_log(LOG_ERR,"error setting index while loading "
                    "ChannelGeneralFaultStateTable data.\n");
            ChannelGeneralFaultStateTable_release_rowreq_ctx(rowreq_ctx);
            continue;
        }

        /*
        * TODO:352:r: |   |-> populate ChannelGeneralFaultStateTable data context.
        * Populate data context here. (optionally, delay until row prep)
        */
        /*
        * TRANSIENT or semi-TRANSIENT data:
        * copy data or save any info needed to do it in row_prep.
        */
        /*
        * setup/save data for channelFaults
        * channelFaults(2)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/R/d/h
        */
        /** no mapping */
        rowreq_ctx->data.channelFaults = table_ptr->data.channelFaults;
        
            
        /*
        * insert into table container
        */
        CONTAINER_INSERT(container, rowreq_ctx);
        ++count;
    }

    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/

    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    DEBUGMSGT(("verbose:ChannelGeneralFaultStateTable:ChannelGeneralFaultStateTable_container_load",
               "inserted %" NETSNMP_PRIz "u records\n", count));

    return MFD_SUCCESS;
} /* ChannelGeneralFaultStateTable_container_load */

/**
 * container clean up
 *
 * @param container container with all current items
 *
 *  This optional callback is called prior to all
 *  item's being removed from the container. If you
 *  need to do any processing before that, do it here.
 *
 * @note
 *  The MFD helper will take care of releasing all the row contexts.
 *
 */
void
ChannelGeneralFaultStateTable_container_free(netsnmp_container *container)
{
    DEBUGMSGTL(("verbose:ChannelGeneralFaultStateTable:ChannelGeneralFaultStateTable_container_free","called\n"));

    /*
     * TODO:380:M: Free ChannelGeneralFaultStateTable container data.
     */
} /* ChannelGeneralFaultStateTable_container_free */

/**
 * prepare row for processing.
 *
 *  When the agent has located the row for a request, this function is
 *  called to prepare the row for processing. If you fully populated
 *  the data context during the index setup phase, you may not need to
 *  do anything.
 *
 * @param rowreq_ctx pointer to a context.
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 */
int
ChannelGeneralFaultStateTable_row_prep( ChannelGeneralFaultStateTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:ChannelGeneralFaultStateTable:ChannelGeneralFaultStateTable_row_prep","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:390:o: Prepare row for request.
     * If populating row data was delayed, this is the place to
     * fill in the row for this request.
     */

    return MFD_SUCCESS;
} /* ChannelGeneralFaultStateTable_row_prep */

/** @} */
