/*
 * Note: this file originally auto-generated by mib2c
 * using mfd-top.m2c
 */
/** \page MFD helper for trafficLightFaultStatusTable
 *
 * \section intro Introduction
 * Introductory text.
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "trafficLightFaultStatusTable.h"

#include <net-snmp/agent/mib_modules.h>

#include "trafficLightFaultStatusTable_interface.h"

const oid trafficLightFaultStatusTable_oid[] = { TRAFFICLIGHTFAULTSTATUSTABLE_OID };
const int trafficLightFaultStatusTable_oid_size = OID_LENGTH(trafficLightFaultStatusTable_oid);

    trafficLightFaultStatusTable_registration  trafficLightFaultStatusTable_user_context;

void initialize_table_trafficLightFaultStatusTable(void);
void shutdown_table_trafficLightFaultStatusTable(void);


/**
 * Initializes the trafficLightFaultStatusTable module
 */
void
init_trafficLightFaultStatusTable(void)
{
    DEBUGMSGTL(("verbose:trafficLightFaultStatusTable:init_trafficLightFaultStatusTable","called\n"));

    /*
     * TODO:300:o: Perform trafficLightFaultStatusTable one-time module initialization.
     */
     
    /*
     * here we initialize all the tables we're planning on supporting
     */
    if (should_init("trafficLightFaultStatusTable"))
        initialize_table_trafficLightFaultStatusTable();

} /* init_trafficLightFaultStatusTable */

/**
 * Shut-down the trafficLightFaultStatusTable module (agent is exiting)
 */
void
shutdown_trafficLightFaultStatusTable(void)
{
    if (should_init("trafficLightFaultStatusTable"))
        shutdown_table_trafficLightFaultStatusTable();

}

/**
 * Initialize the table trafficLightFaultStatusTable 
 *    (Define its contents and how it's structured)
 */
void
initialize_table_trafficLightFaultStatusTable(void)
{
    trafficLightFaultStatusTable_registration * user_context;
    u_long flags;

    DEBUGMSGTL(("verbose:trafficLightFaultStatusTable:initialize_table_trafficLightFaultStatusTable","called\n"));

    /*
     * TODO:301:o: Perform trafficLightFaultStatusTable one-time table initialization.
     */

    /*
     * TODO:302:o: |->Initialize trafficLightFaultStatusTable user context
     * if you'd like to pass in a pointer to some data for this
     * table, allocate or set it up here.
     */
    /*
     * a netsnmp_data_list is a simple way to store void pointers. A simple
     * string token is used to add, find or remove pointers.
     */
    user_context = netsnmp_create_data_list("trafficLightFaultStatusTable", NULL, NULL);
    
    /*
     * No support for any flags yet, but in the future you would
     * set any flags here.
     */
    flags = 0;
    
    /*
     * call interface initialization code
     */
    _trafficLightFaultStatusTable_initialize_interface(user_context, flags);
} /* initialize_table_trafficLightFaultStatusTable */

/**
 * Shutdown the table trafficLightFaultStatusTable 
 */
void
shutdown_table_trafficLightFaultStatusTable(void)
{
    /*
     * call interface shutdown code
     */
    _trafficLightFaultStatusTable_shutdown_interface(&trafficLightFaultStatusTable_user_context);
}

/**
 * extra context initialization (eg default values)
 *
 * @param rowreq_ctx    : row request context
 * @param user_init_ctx : void pointer for user (parameter to rowreq_ctx_allocate)
 *
 * @retval MFD_SUCCESS  : no errors
 * @retval MFD_ERROR    : error (context allocate will fail)
 */
int
trafficLightFaultStatusTable_rowreq_ctx_init(trafficLightFaultStatusTable_rowreq_ctx *rowreq_ctx,
                           void *user_init_ctx)
{
    DEBUGMSGTL(("verbose:trafficLightFaultStatusTable:trafficLightFaultStatusTable_rowreq_ctx_init","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    
    /*
     * TODO:210:o: |-> Perform extra trafficLightFaultStatusTable rowreq initialization. (eg DEFVALS)
     */

    return MFD_SUCCESS;
} /* trafficLightFaultStatusTable_rowreq_ctx_init */

/**
 * extra context cleanup
 *
 */
void trafficLightFaultStatusTable_rowreq_ctx_cleanup(trafficLightFaultStatusTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:trafficLightFaultStatusTable:trafficLightFaultStatusTable_rowreq_ctx_cleanup","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    
    /*
     * TODO:211:o: |-> Perform extra trafficLightFaultStatusTable rowreq cleanup.
     */
} /* trafficLightFaultStatusTable_rowreq_ctx_cleanup */

/**
 * pre-request callback
 *
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_ERROR                : other error
 */
int
trafficLightFaultStatusTable_pre_request(trafficLightFaultStatusTable_registration * user_context)
{
    DEBUGMSGTL(("verbose:trafficLightFaultStatusTable:trafficLightFaultStatusTable_pre_request","called\n"));

    /*
     * TODO:510:o: Perform trafficLightFaultStatusTable pre-request actions.
     */

    return MFD_SUCCESS;
} /* trafficLightFaultStatusTable_pre_request */

/**
 * post-request callback
 *
 * Note:
 *   New rows have been inserted into the container, and
 *   deleted rows have been removed from the container and
 *   released.
 *
 * @param user_context
 * @param rc : MFD_SUCCESS if all requests succeeded
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : other error (ignored)
 */
int
trafficLightFaultStatusTable_post_request(trafficLightFaultStatusTable_registration * user_context, int rc)
{
    DEBUGMSGTL(("verbose:trafficLightFaultStatusTable:trafficLightFaultStatusTable_post_request","called\n"));

    /*
     * TODO:511:o: Perform trafficLightFaultStatusTable post-request actions.
     */

    return MFD_SUCCESS;
} /* trafficLightFaultStatusTable_post_request */


/** @{ */
