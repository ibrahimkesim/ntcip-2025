/*
 * Note: this file originally auto-generated by mib2c
 * using mfd-data-get.m2c
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "userOperationsTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement userOperationsTable get routines.
 * TODO:240:M: Implement userOperationsTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table userOperationsTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * kgm-ortak::userOperationsTable is subid 2 of user.
 * Its status is Mandatory.
 * OID: .1.3.6.1.4.1.59873.4.2.1.5.2, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement userOperationsTable data context functions.
 */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 * @param userOperationsIndex_val
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
userOperationsTable_indexes_set_tbl_idx(userOperationsTable_mib_index *tbl_idx, long userOperationsIndex_val)
{
    DEBUGMSGTL(("verbose:userOperationsTable:userOperationsTable_indexes_set_tbl_idx","called\n"));

    /* userOperationsIndex(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/R/d/h */
    tbl_idx->userOperationsIndex = userOperationsIndex_val;
    

    return MFD_SUCCESS;
} /* userOperationsTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
userOperationsTable_indexes_set(userOperationsTable_rowreq_ctx *rowreq_ctx, long userOperationsIndex_val)
{
    DEBUGMSGTL(("verbose:userOperationsTable:userOperationsTable_indexes_set","called\n"));

    if(MFD_SUCCESS != userOperationsTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , userOperationsIndex_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != userOperationsTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* userOperationsTable_indexes_set */


/*---------------------------------------------------------------------
 * kgm-ortak::userOperationsEntry.userOperationsType
 * userOperationsType is subid 2 of userOperationsEntry.
 * Its status is Mandatory, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.59873.4.2.1.5.2.1.2
 * Description:
<Definition>

		userAdded: Kullanıcı eklendi
		timeChanged: Kullanıcı tarih saat değiştirdi
		outputRelaysEnabled: Çıkış rolesi devrede
		outputRelaysDisabled: Çıkış rölesi devre dışı
		userLogin: Kullanıcı sisteme giriş yaptı
		userLogout: Kullanıcı sistemden çıkış yaptı
		modeChanged: kullanıcı tarafından mod değiştirildi.
		deviceParamatersChanged: cihaz paramatreleri değiştirildi.

	<Object Identifier> KGM OID
	<Unit> 
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 * Enum range: 9. Values:  none(0), userAdded(1), timeChanged(2), outputRelaysEnabled(3), outputRelaysDisabled(4), userLogin(5), userLogout(6), modeChanged(7), deviceParamatersChanged(8)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the userOperationsType data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param userOperationsType_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
userOperationsType_get( userOperationsTable_rowreq_ctx *rowreq_ctx, u_long * userOperationsType_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != userOperationsType_val_ptr );


    DEBUGMSGTL(("verbose:userOperationsTable:userOperationsType_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the userOperationsType data.
 * copy (* userOperationsType_val_ptr ) from rowreq_ctx->data
 */
    (* userOperationsType_val_ptr ) = rowreq_ctx->data.userOperationsType;

    return MFD_SUCCESS;
} /* userOperationsType_get */

/*---------------------------------------------------------------------
 * kgm-ortak::userOperationsEntry.userOperationsTime
 * userOperationsTime is subid 3 of userOperationsEntry.
 * Its status is Mandatory, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.59873.4.2.1.5.2.1.3
 * Description:
<Definition>
		Kullanıcının işlem yaptığı zamanın bilgisi.
<Object Identifier> KGM OID
<Unit>  
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is COUNTER (based on perltype COUNTER)
 * The net-snmp type is ASN_COUNTER. The C type decl is u_long (u_long)
 */
/**
 * Extract the current value of the userOperationsTime data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param userOperationsTime_val_ptr
 *        Pointer to storage for a u_long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
userOperationsTime_get( userOperationsTable_rowreq_ctx *rowreq_ctx, u_long * userOperationsTime_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != userOperationsTime_val_ptr );


    DEBUGMSGTL(("verbose:userOperationsTable:userOperationsTime_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the userOperationsTime data.
 * copy (* userOperationsTime_val_ptr ) from rowreq_ctx->data
 */
    (* userOperationsTime_val_ptr ) = rowreq_ctx->data.userOperationsTime;

    return MFD_SUCCESS;
} /* userOperationsTime_get */

/*---------------------------------------------------------------------
 * kgm-ortak::userOperationsEntry.userOperationsULoginName
 * userOperationsULoginName is subid 4 of userOperationsEntry.
 * Its status is Mandatory, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.59873.4.2.1.5.2.1.4
 * Description:
<Definition>
		İşlem yapan kullanıcının kullanıcı adı bilgisi.
<Object Identifier> KGM OID
<Unit>  
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the userOperationsULoginName data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param userOperationsULoginName_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param userOperationsULoginName_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by userOperationsULoginName.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*userOperationsULoginName_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update userOperationsULoginName_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
userOperationsULoginName_get( userOperationsTable_rowreq_ctx *rowreq_ctx, char **userOperationsULoginName_val_ptr_ptr, size_t *userOperationsULoginName_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != userOperationsULoginName_val_ptr_ptr) && (NULL != *userOperationsULoginName_val_ptr_ptr));
   netsnmp_assert( NULL != userOperationsULoginName_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:userOperationsTable:userOperationsULoginName_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the userOperationsULoginName data.
 * copy (* userOperationsULoginName_val_ptr_ptr ) data and (* userOperationsULoginName_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for userOperationsULoginName data
     */
    if ((NULL == (* userOperationsULoginName_val_ptr_ptr )) ||
        ((* userOperationsULoginName_val_ptr_len_ptr ) <
         (rowreq_ctx->data.userOperationsULoginName_len* sizeof(rowreq_ctx->data.userOperationsULoginName[0])))) {
        /*
         * allocate space for userOperationsULoginName data
         */
        (* userOperationsULoginName_val_ptr_ptr ) = malloc(rowreq_ctx->data.userOperationsULoginName_len* sizeof(rowreq_ctx->data.userOperationsULoginName[0]));
        if(NULL == (* userOperationsULoginName_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.userOperationsULoginName)\n");
            return MFD_ERROR;
        }
    }
    (* userOperationsULoginName_val_ptr_len_ptr ) = rowreq_ctx->data.userOperationsULoginName_len* sizeof(rowreq_ctx->data.userOperationsULoginName[0]);
    memcpy( (* userOperationsULoginName_val_ptr_ptr ), rowreq_ctx->data.userOperationsULoginName, rowreq_ctx->data.userOperationsULoginName_len* sizeof(rowreq_ctx->data.userOperationsULoginName[0]) );

    return MFD_SUCCESS;
} /* userOperationsULoginName_get */



/** @} */
